---
status: candidate-1
review_mode: ultraverge
object_slug: 20260912-o6-material-delta-recert
---

# O6 · material 修订增量复核机制（candidate-1）

> 本计划是操作包络层 O6（`docs/plans/active/20260911-converge-operational-envelope.md:25`）的重评 +（评议支持时）机制实现。
> **触及第三部**：`refs/state-schema.md`、`refs/orchestrator-guide.md`、`SKILL.md`（清单 `CONSTITUTION.md:65-78`；`:71-72` 为 state-schema/orchestrator-guide）→ 授权源 ultraverge。本计划自身内嵌 `converge.governance-change/v1` 机器块（§14）并以 preflight 自验。
> 全部行号/符号均实开文件核对；证据见 §2。candidate-1 全文 ≤ 45KB 由 §7 A-1 机械验收。

## §0 目录

| 节 | 内容 |
|---|---|
| §1 | Goal |
| §2 | 证据（B 成本 + 材料门实核） |
| §3 | D1：material 修订分级 + 增量复核路径 |
| §4 | D2：O6 决议协议 |
| §5 | 变更落点 Exact File Matrix |
| §6 | Bounded Sequence |
| §7 | Acceptance（机械可判定） |
| §8 | Non-Goals |
| §9 | 预算与止损 |
| §10 | 风险与残留 |
| §11 | 对抗回归清单 |
| §12 | 规范句逐字对照表 |
| §13 | 回滚 |
| §14 | 附录 A：`converge.governance-change/v1` 机器块 |

---

## §1 Goal

1. **重评 O6**：以 B 对象的实测成本为 calibration 样本，重新判断"material revision 增量复核"是否值得作为机制固化（此前 B §14 为 record-only）。
2. **机制实现（评议支持时）**：在**不放宽 decisional 修订全量双权威同字节重认证**的前提下，为纯 `non-decisional` material 修订引入**单 fresh delta reviewer** 增量路径，消除"任何字节变化都触发全量双认证"的成本爆炸。
3. **可机械验证**：材料门 fail-closed、反向对抗用例、逐字规范改写均可由测试/脚本判定；判断类事项（归类是否正确）保留在 agent 手中。

**成功判据**：材料门在 `decisional` 链段仍强制全量对；在 `non-decisional` 链段以 hash 链 + delta payload + verdict 三重机械校验替代第二权威；任何一环缺失一律 `FAIL_CLOSED`，不降级。

---

## §2 证据（实核，逐条可核）

### 2.1 成本数据（B 对象）

| 事实 | 出处（实核） | 值 |
|---|---|---|
| 计划多轮修订触发全量双认证 | `.converge/done/20260911-op-envelope-b-contract-correction/retrospective.md:75` | "计划字节 41KB→133KB；每轮字节变更触发全量双认证" |
| 重认证成本占比 | 同上 `:80`（§8 经验教训） | "7 次重认证 ≈ 全部评议成本的一半" |
| 重认证轮序 | 同上 `:53`（§5） | R1-R5 + blind 1-4 中多次同字节复核（材料对 #1/#2） |
| 计划体积 | 同上 `:42`（§3）、`:75` | candidate-1→8：41KB→133KB |
| O6 议题来源 | `docs/plans/active/20260911-converge-operational-envelope.md:25` | "material revision 无 delta 复核语义；D8 每次字节变更重跑双 fresh 同字节复核" |

### 2.2 材料门实现（`scripts/orchest.py`，实核）

| 符号/锚点 | 行号 | 说明 |
|---|---|---|
| `_find_material_block` | `:1095` | 读 `attempts.md` 的 `converge.material-revision/v1` fence；无 id 取**最后一块**（append-only，last-supersedes-all）；返回块 + canonical sha256（`:1116-1117`） |
| `_validate_material_gate` | `:1133` | finish 步骤 3.5 调用 |
| finish 调用点 | `:1579-1581` | `_validate_material_gate(active, events)` + `_validate_calibration_sample`（non-dry-run） |
| 锚点(a) 当前块 vs 盘上 plan | `:1171-1175` | `candidate_plan`/`candidate_artifact` 的 `sha256`/`size` 必须等于盘上 `plan.md` |
| 候选集合 | `:1185-1186` | fresh = `REVIEWER_AUTHORITIES["fresh"]`；blank = `["blank-slate"]` |
| exact 证据 + 块抽取 | `:1207-1259` | blob 存在、非空、非 CRLF、prompt/output 各恰一个 `converge.review-target/v1`、两 payload byte-equal |
| 锚点(b) 候选 payload vs 盘上 plan | `:1267-1272` | `artifact.sha256/size` 必须等于盘上 plan |
| 锚点(c) 候选 vs 当前 material 块 | `:1273-1279` | `material_revision.sha256` 必须等于当前块 canonical hash；locator id 一致 `:1280-1293` |
| 全量对要求 | `:1303-1310` | 至少一个 fresh + 一个 blank-slate 合格候选，否则 `FAIL_CLOSED` |
| 两 payload byte-equal | `:1335-1337` | fresh vs blank payload 必须逐字节相同 |
| verdict 非阻断检查 | `:1340-1361` | 任一输出 `verdict: 阻断需修复` → `FAIL_CLOSED`；并核对 round 产物 frontmatter |
| legacy 跳过语义 | `:1144-1149`、`:1204-1205` | 非合格 legacy terminal 跳过（不 fail），但**无合格对仍 fail** |

### 2.3 规范句（第三部，实核）

| 位置 | 内容 |
|---|---|
| `refs/state-schema.md:101-104` | `converge.review-target/v1` payload 规范；`:104` 即"任何字节变化使两份审查同时失效"句 |
| `refs/orchestrator-guide.md:17-32` | §Material revision：`:23` triggers、`:27-31` 同字节两-Spawn 流程、`:32` 失效句 |
| `SKILL.md:462` | "Material revision 两-authority 同字节规则"指针句（与本机制一致性相关，实核**需一并改写**） |
| `CONSTITUTION.md:71-72` | 第三部清单（state-schema / orchestrator-guide） |

### 2.4 calibration（实核）

本对象 `evidence/calibration-report.json` 已由生成器产出（`converge.calibration-report/v1`）：
`canonical sha256=16ec2455acb9cdb143145e8f28c52b4db1d96f9cd90b44ab4b8af897df3303c8`；
`corpus_digest=3627eb4d72cfbf07a3c1a6655026c9ce51063506e613c3a7df5b3519b10224ea`；
`freshness={repository_head=da81e70cee7931d49680866f9cd0b2cdb6fadc5a, source_archive_revision=unavailable, source_event_high_watermark=0}`；
`scope=done-corpus`、corpus 20 entries 全 `unavailable/no_sample`、`eligible_samples=0`。
→ O6 是 **mechanism-kind** 变更，不经数值经验门（校验逻辑 `scripts/budget_gate.py:1682-1699`；窄门 `:1710-1742` 仅裁决 `default/threshold/stopping_condition` 且 `comparison∈{outer,blind}`）。
calibration 的 locator 载体：`attempts.md::json-fence[schema=converge.calibration-report/v1,id=calibration]`（canonical 字节与 `evidence/calibration-report.json` 逐字节一致）；独立文件是证据留痕，不被 preflight 机械校验（与 B 对象同构；locator `root-file` 必须在 Archive Contract 根 allowlist 内、不含路径分隔符——`refs/state-schema.md:76`、`scripts/budget_gate.py:1452-1459`，故 `evidence/` 路径不可寻址）。

---

## §3 D1：material 修订分级（change_class）+ 增量复核路径

### 3.1 机器块新字段

`converge.material-revision/v1` 块新增：

| 字段 | 类型 | 必填 | 语义 |
|---|---|---|---|
| `change_class` | `"decisional" \| "non-decisional"` | 新块必填 | 修订分级 |
| `changed_sections` | string 列表（非空） | 新块必填 | 本次修订触及的章节标识（受控词表，见 3.2） |
| `decisional_anchors` | string 列表（非空） | `decisional` 块必填 | 该 decision 内容所在的章节标识集合（供后续 non-decisional 块做交集 fail-safe） |

字段缺省规则（前向兼容）：无 `change_class` 的旧块视为 `decisional`；无 `decisional_anchors` 的旧 decisional 块视为 `decisional_anchors = ["*"]`（全锚定，任何 non-decisional 变更都会触发交集非空 → 保守 fail-closed 到全量对）。`_find_material_block` 的"最后一块"语义**不变**。

### 3.2 判定标准（写入计划，并由 §12 规范化进 `refs/orchestrator-guide.md`）

**`decisional`** = 触及判定正确性，至少包含：
- 数值默认 / 阈值 / 停止条件；
- 角色与权限（含 `REVIEWER_AUTHORITIES`、consumes 归属）；
- verdict 语义与三档字面量；
- fail-closed 行为、错误码 / 退出码 / 失败分类；
- File Matrix（改动落点集合）；
- Acceptance（验收断言）；
- D 决策本体（本节及后续 `Dn` 决策文本）。

**`non-decisional`** = 不影响上列判定：行号 / 符号引用更新、措辞、附录、与判定无关的文档同步、测试内非断言语义调整。

**受控章节词表**（`changed_sections` / `decisional_anchors` 取值）：`D-decisions`、`file-matrix`、`acceptance`、`triggers`、`verdict-semantics`、`roles-permissions`、`fail-closed`、`numeric-defaults`、`doc-refs`、`wording`、`appendix`、`tests-non-assertive`。上表前缀 `decisional` 覆盖的章节一律落在前 8 项；`non-decisional` 只允许后 4 项。

**归类权责**：plan 作者**声明**、复核者**挑战**（判断留在 agent）；**归类争议 = decisional**（fail-safe）。任一 reviewer 指出归类错误 → 该块按 decisional 重做全量对。

### 3.3 `decisional` 路径（不变）

沿用现行全量双权威同字节重认证：两个不同 fresh Spawn（`outer-reviewer` + `blank-slate`，exact 证据），review-target payload 不含 `delta`，两 payload byte-equal，verdict 零阻断。实现上即现 `_validate_material_gate` 的 full-pair 分支，**不回退、不放宽**。

### 3.4 `non-decisional` 路径：单 fresh delta reviewer

- 用**一个** fresh Spawn（复用 `outer-reviewer` 角色，exact 证据）替代双权威。
- review-target payload 增加 `delta{base_plan_sha256, current_plan_sha256, declared_class}`：
  - `base_plan_sha256` = 链上**前一有效字节**（前一块 `candidate_artifact.sha256`）的哈希；
  - `current_plan_sha256` = 本块 `candidate_artifact.sha256`（末块须等于盘上 `plan.md`）；
  - `declared_class` = 本块 `change_class`。
- 职责：**实核 diff**（前一块 bytes → 本块 bytes），确认未触及 decisional 内容且上一轮 blocking 项已闭合；verdict 必须为 `可执行`。
- 该 reviewer 的 prompt/output 仍各恰含一个 `converge.review-target/v1` 块，两者 canonical payload byte-equal。

### 3.5 材料门改造（`_validate_material_gate`）

链 = `attempts.md` 中全部 `converge.material-revision/v1` 块（文档顺序）。伪代码：

```text
blocks = all material blocks (document order)
if empty: return                          # 向后兼容
current = blocks[-1]
validate current.candidate_artifact == disk plan (sha256/size)   # 原 :1171-1175
i = max index with blocks[i].change_class(default=decisional) == decisional
if i is None:                             # 全 non-decisional 链（异常/旧形态）
    require full fresh+blank pair qualified against current        # 保守=现状
    return
require full fresh+blank pair qualified against blocks[i]          # decisional 段全量对
for j in (i+1 .. len(blocks)-1):
    if blocks[j].change_class == decisional: continue              # 新的 decisional 段
    require a delta candidate for blocks[j]:
        payload.delta.declared_class == blocks[j].change_class
        payload.delta.base_plan_sha256 == blocks[j-1].candidate_artifact.sha256
        payload.delta.current_plan_sha256 == blocks[j].candidate_artifact.sha256
        role in REVIEWER_AUTHORITIES["fresh"], exact evidence
        output echoes payload byte-equal
        verdict != 阻断需修复
    require blocks[j].changed_sections ∩ blocks[i].decisional_anchors == ∅
    any missing link -> FAIL_CLOSED (no downgrade)
```

实现要点：
- full-pair 校验逻辑从"只对 current 块"泛化为"对指定 target 块"（新增内部参数/helper），使 decisional 段可对 `blocks[i]` 校验。
- 原 legacy 跳过语义（`:1144-1149`）保留：非合格候选跳过，但**所要求的那一类**（full 对或 delta 候选）缺失即 fail。
- 交集 fail-safe：`changed_sections ∩ decisional_anchors ≠ ∅` 且 `change_class=non-decisional` → `FAIL_CLOSED:material-gate:non-decisional-touches-decisional-anchor`（对抗用例 A-11-4 的机械拦截点）。

### 3.6 fail-closed 与争议

- 任何"链断、hash 不匹配、payload 不一致、verdict 阻断、交集非空、归类争议" → `FAIL_CLOSED`。
- **不降级**：不得因"没找到 delta 候选"而回退为放行；不得因"delta reviewer 未抓到"而机械放行后续 full 要求。
- delta 路径仅在**明确** non-decisional 且机械交集为空时生效；模糊时 fail closed。

### 3.7 前向兼容

- 旧块无 `change_class` → `decisional`（保守）。
- 旧 `review-target` payload 无 `delta` → 仅可用于 full-pair；delta 候选必须含合法 `delta`。
- 现有 7 个 material 测试（`tests/test_loop_a_coverage.py:443-1421` 区间）行为不变；新增测试不得改变它们的断言。

---

## §4 D2：O6 决议协议

本对象是"重评 + 条件实现"，机制**是否实现**由评议结论机械分流：

1. 若三名 ultraverge 初审**一致否定**增量路径（`verdict=需重新设计`，或 conceptual/architectural 阻断指向 D1 方向）→ **record-only 收口**：本对象只沉淀"O6 重评结论 + 分级判定标准 + 不实现理由"，**不修改** `orchest.py`/`state-schema`/`guide`。冻结产物 = 修订后的计划 + retrospective；`decisional` 全量规则保持原状。
2. 若**多数支持**但少数派含 conceptual/architectural 阻断 → 按 `SKILL.md:170` ultraverge 并行裁决规则**升级完整收敛**，不得以多数决跳过深层阻断。
3. 若 verdict `可执行`（或收敛后达成）→ 进入 §6 执行序列。
4. 收敛判定遵循 2026-09-12 用户裁决（`~/.config/opencode/memories.md`）：评审趋势收敛 + 达限即通过；未决小问题转实施约束清单或留待下次。该裁决**不放宽**本计划 §7 的机械 Acceptance。

---

## §5 变更落点 Exact File Matrix

| # | 文件 | 动作 | 锚点（实核行号） | 说明 |
|---|---|---|---|---|
| F1 | `scripts/orchest.py` | 改造 | `_validate_material_gate` `:1133-1361`；`_find_material_block` `:1095-1118` 不变；finish 调用 `:1579-1581` 不变 | 链回溯 + delta 候选 + full-pair 泛化为 target-block；新增 `change_class` 缺省；新增 fail-closed 码 |
| F2 | `refs/state-schema.md`（第三部） | 改写 | `:101-104` | review-target payload 增 `delta`；material 块增 `change_class/changed_sections/decisional_anchors`；重写 `:104` 失效句（§12 逐字） |
| F3 | `refs/orchestrator-guide.md`（第三部） | 改写 | `:17-32`（`:23` triggers、`:25-32` 流程） | 增分级判定标准 + delta 流程；改写 `:32` 失效句（§12 逐字） |
| F4 | `SKILL.md`（第三部） | 改写 | `:462` | 两-authority 指针句补 non-decisional delta 例外（§12 逐字；实核增补） |
| F5 | `tests/test_loop_a_coverage.py` | 新增测试 | 现 `TestMaterialClosureGate` `:351` / `TestMaterialLocatorResolution` `:848` / `TestMaterialGateQualifyByCurrentBlock` `:1184` | 新增 `TestMaterialDeltaPath`（delta 通过/失败/链断/交集/verdict）+ §11 对抗用例 |
| F6 | `tests/test_process_controller_contract.py` | 调整 + 新增 | `test_orchest_negative_read_side_default_untouched` `:214-218`；写点契约 `:199-212` | 确保改造后 `_validate_material_gate` 仍含 `metadata-only` 负例；新增"分级常量单源在 `orchest.py`"静态断言 |
| F7 | `scripts/README.md` | 文档 | `:144-151`（evidence mode 段，实核） | 记录 delta 路径需 exact 证据；命令流与两-authority 并列 |

> F4 为实核**增补**：`SKILL.md:462` 的规范性指针句若不改，将与新语义矛盾（第三部一致性）。F6 为实核**增补**：`test_orchest_negative_read_side_default_untouched` 直接锚定 `_validate_material_gate` 源码，改造后必须保持其断言成立或显式更新。

---

## §6 Bounded Sequence

| 阶段 | 步骤 | 机械验收 |
|---|---|---|
| P0 准备 | 配置/确认对象 active 目录 task-envelope（已具备：`_budget-state.json` `task_tier=critical`） | `python scripts/budget_gate.py preflight --plan <plan>` → `PREFLIGHT_OK:governance-change` |
| P1 代码 | F1：改造 `_validate_material_gate`；`py_compile scripts/orchest.py` | 退出码 0 |
| P2 规范 | F2/F3/F4：逐字应用 §12 对照 | `git diff` 与 §12 新句完全一致 |
| P3 测试 | F5/F6：新增 delta + 对抗用例 | `python -m pytest tests/test_loop_a_coverage.py tests/test_process_controller_contract.py -q` 全绿 |
| P4 全量 | 全量回归 | `python -m pytest -q` 全绿；`git diff --check` 通过 |
| P5 文档 | F7 | `scripts/README.md` 含 delta 流程段 |
| P6 收口 | 写 retrospective + `orchest.py finish` | finish 步骤 3.5 对**本对象**材料门通过；全 settle 无孤儿 |

> P0 的 preflight 是 candidate-1 自身自验；P1-P5 由落地执行 executor 完成（宪法 #7：orchestrator 不直接改产物）。

---

## §7 Acceptance（机械可判定）

| ID | 断言 | 判定方式 |
|---|---|---|
| A-1 | candidate-1 `plan.md` ≤ 45KB | 文件字节数 ≤ 46080 |
| A-2 | frontmatter 含 `status: candidate-1`、`review_mode: ultraverge`、`object_slug: 20260912-o6-material-delta-recert` | 头 6 行逐字包含 |
| A-3 | 治理机器块 preflight 通过 | `budget_gate.py preflight --plan <plan>` 含 `PREFLIGHT_OK:governance-change`（实跑输出见 `attempts.md`） |
| A-4 | plan.md 恰含一个 `converge.governance-change/v1` fence | 计数 == 1 |
| A-5 | `change_class` 缺省 = decisional | 新测试：旧块（无字段）走 full-pair；`grep change_class` 命中缺省逻辑 |
| A-6 | decisional 链段仍要求 fresh+blank 全量对 | 新测试：decisional 块缺 blank-slate → `FAIL_CLOSED` |
| A-7 | δ 路径缺候选 → `FAIL_CLOSED` | 新测试：non-decisional 块无 delta reviewer → fail |
| A-8 | δ base hash 不匹配 → `FAIL_CLOSED` | 新测试（A-11-2） |
| A-9 | δ verdict 阻断 → `FAIL_CLOSED` | 新测试；覆盖 `:1340-1361` 语义 |
| A-10 | 交集 fail-safe：`changed_sections ∩ decisional_anchors ≠ ∅` 的 non-decisional 块 → `FAIL_CLOSED` | 新测试（A-11-4 机械拦截） |
| A-11 | §11 对抗用例全部被拦 | 各用例断言 `rc != 0` / 指定错误码 |
| A-12 | 现有 material 测试不改断言仍全绿 | `git diff` 未改 `tests/test_loop_a_coverage.py:443-1421` 既有断言；pytest 全绿 |
| A-13 | §12 逐字对照全部落实 | 逐条 grep 新句存在、旧句消失 |
| A-14 | §5 全部文件被触达 | `git status --porcelain` 覆盖 F1-F7 |
| A-15 | 无新增硬编码 read-side 默认 | `test_process_controller_contract.py` 静态断言通过 |
| A-16 | 全量测试绿 | `python -m pytest -q` 退出码 0 |
| A-17 | 不改任何数值默认/阈值/停止条件 | `git diff` 中 `budget_gate.py` 无改动；机器块 `numeric_changes` 全 `kind: mechanism` |
| A-18 | 回滚可执行 | §13 回滚步骤对当前 HEAD 有效（`git checkout -- <F1-F4>` 无冲突） |

---

## §8 Non-Goals

1. **不放宽 `decisional` 全量规则**：D1.3 原样保留，任何 decisional 修订仍须两 fresh 同字节对。
2. **不改预算限额 / `budget_gate.py`**：8/3/3、task-envelope 档位、总量公式均不动。
3. **不新增 O6 否定路径的机制**：若评议否定，仅 record-only，不实现降级/半成品。
4. **不改 `_find_material_block` 的 last-supersedes-all 语义**。
5. **不引入跨厂商多样性要求**：delta reviewer 仍是 fresh 独立上下文（与现状一致）。
6. **不动 `converge.review-target/v1` 既有字段语义**，只**增** `delta`。

---

## §9 预算与止损

- ultraverge 初审固定 3 名（`ultraverge_min_reviewers=3`）；本对象 active 状态 `fsm.mode=ultraverge`、`task_envelope=critical(20/30)`（实核 `_budget-state.json`）。
- 收敛止损依 `memories.md` 约定：趋势收敛（阻断数与严重度单调不增、无新增 conceptual/architectural、余项精度级）时达限即通过，小问题转实施约束清单。
- 未决小问题清单在 retrospective 落账，由实施后独立 fresh 审计兜底。

---

## §10 风险与残留

| # | 风险 | 缓解 |
|---|---|---|
| R-1 | 作者谎报 `changed_sections`（漏报 decisional 章节）绕过交集检查 | 交集检查只拦"声明了 decisional 章节"的情形；语义漏报由 fresh delta reviewer 的 diff 实核 + "归类争议=decisional"兜底；下一 decisional 修订强制全量对 |
| R-2 | delta reviewer 被锚定而放行 | fresh 独立上下文；payload 强制回显；verdict 非阻断检查；争议 fail-safe |
| R-3 | 链回溯实现错误导致误放行 | A-6/A-7/A-11 对抗用例；fail-closed 缺省 |
| R-4 | 规范句未同步致文档-实现漂移 | A-13 逐字 grep；F4 一致性 |
| R-5 | 计划膨胀（B 的 133KB 教训） | A-1 ≤45KB；论证性文字从简 |

---

## §11 对抗回归清单（用例须全部被拦）

| ID | 攻击 | 预期机械拦截 |
|---|---|---|
| A-11-1 | 谎报 `non-decisional` 但 diff 触及 decisional，delta reviewer 正确判阻断 | reviewer verdict=`阻断需修复` → `:1340-1361` verdict 校验 FailClosed；升级全量 |
| A-11-2 | delta payload 伪造 `base_plan_sha256` | 链 hash 校验 `delta.base_plan_sha256 != blocks[j-1].candidate_artifact.sha256` → FailClosed |
| A-11-3 | 链中删除一个 non-decisional 块 | 链回溯缺环（后块 base hash 无对应前块）→ FailClosed |
| A-11-4 | decisional 误标 non-decisional，且 `changed_sections` 含 decisional 章节 | 交集 fail-safe（A-10）→ FailClosed |
| A-11-5 | delta payload `declared_class` ≠ 块 `change_class` | payload 一致性校验 → FailClosed |
| A-11-6 | delta payload 与 reviewer 输出回显不一致 | byte-equal 校验 → FailClosed |
| A-11-7 | decisional 段缺 blank-slate 权威 | full-pair 校验 → FailClosed |
| A-11-8 | 旧块无 `change_class` 被当作 non-decisional 放行 | 缺省 decisional → 要求 full-pair；无对 → FailClosed |

> A-11-4 是本设计对"decisional 误标 + reviewer 放行"的**机械**拦截：`changed_sections` 与 `decisional_anchors` 的交集判定不依赖 reviewer verdict。若攻击者进一步漏报 `changed_sections`，则落入 R-1：该残余由语义层（fresh delta reviewer + 争议 fail-safe）承担，计划**如实声明不声称机械闭环**。

---

## §12 规范句逐字对照表

> 左 = 实核原文（含行号）；右 = 替换文本。替换后须 `grep` 新句存在、旧句消失（A-13）。

### 12.1 `refs/state-schema.md:103`

- 原文（`:103`）：
  `- 字段：`schema`、`target_id`、`revision_id`、`artifact{path,sha256,size}`、`material_revision{locator,sha256}`（locator 指向 `attempts.md` 内唯一 `converge.material-revision/v1` 块）、`quality_goal_event_id`（UUID）。`
- 替换为：
  `- 字段：`schema`、`target_id`、`revision_id`、`artifact{path,sha256,size}`、`material_revision{locator,sha256}`（locator 指向 `attempts.md` 内唯一 `converge.material-revision/v1` 块）、`quality_goal_event_id`（UUID）。`delta` 仅在 non-decisional 增量路径出现，恰含 `{base_plan_sha256,current_plan_sha256,declared_class}`；`declared_class ∈ decisional|non-decisional`，两个 sha256 均为 64 位小写 hex。`
- 新增独立 bullet（紧随 `:103`）：
  `- `converge.material-revision/v1` 块字段：`change_class ∈ decisional|non-decisional`（旧块缺省 = decisional）、`changed_sections`（非空、受控章节词表）、`decisional_anchors`（decisional 块必填；旧块缺省 = ["*"]）。判定标准与受控词表见 `refs/orchestrator-guide.md` §Material revision。`

### 12.2 `refs/state-schema.md:104`

- 原文（`:104`）：
  `- payload 是一行 canonical JSON（sorted keys、compact、UTF-8、恰好一个 LF）。两份 authority prompt 的抽取 canonical payload 字节必须相同，两份 Reviewer 输出回显的 payload 也必须逐字节相同；`plan.md` 事后任何字节变化使两份审查同时失效。`
- 替换为：
  `- payload 是一行 canonical JSON（sorted keys、compact、UTF-8、恰好一个 LF）。decisional 修订：两份 authority prompt 的抽取 canonical payload 字节必须相同，两份 Reviewer 输出回显的 payload 也必须逐字节相同；`plan.md` 事后任何字节变化使两份审查同时失效。non-decisional 修订：单 fresh delta reviewer 的 prompt/output 回显同一 payload，且 `delta.base_plan_sha256` 必须等于链上前一有效字节哈希、`delta.current_plan_sha256` 等于本块候选字节哈希；链上任何一环缺失即 fail closed，不得降级为放宽。`

### 12.3 `refs/orchestrator-guide.md:23`

- 原文（`:23`）：
  `Material triggers 仅限：概念/架构阻断导致的修订、empirical conflict、或核心 numeric default/threshold/stopping condition 变更。纯结构性/实现修复非 material（除非同时变更上述控制项）。模糊时 fail closed。`
- 替换为：
  `Material triggers 仅限：概念/架构阻断导致的修订、empirical conflict、或核心 numeric default/threshold/stopping condition 变更。纯结构性/实现修复非 material（除非同时变更上述控制项）。模糊时 fail closed。`
  `每次 material 修订在块内声明 `change_class` 与 `changed_sections`：`decisional` = 触及数值默认/阈值/停止条件、角色与权限、verdict 语义、fail-closed 行为、File Matrix、Acceptance 或 D 决策本体；`non-decisional` = 行号/符号引用、措辞、附录、与判定无关的文档同步。归类由作者声明、复核者挑战；争议 = decisional。`

### 12.4 `refs/orchestrator-guide.md:32`

- 原文（`:32`）：
  `6. 任何后续 `plan.md` 字节变化使两份审查同时失效。若任一 Reviewer 阻断且 plan 变更，两角色重新审查新 hash。`
- 替换为：
  `6. 任何后续 `plan.md` 字节变化使两份审查同时失效。若任一 Reviewer 阻断且 plan 变更，两角色重新审查新 hash。`
  `7. **non-decisional 增量例外**：`change_class=non-decisional` 的修订不重跑双权威，改由单个 fresh `outer-reviewer`（exact 证据）作 delta 复核：payload 增 `delta{base_plan_sha256,current_plan_sha256,declared_class}`，实核 diff 确未触及 decisional 内容且前轮 blocking 已闭合，verdict 必须 `可执行`。材料门沿块链回溯至最近 `decisional` 块要求全量对，其后每个 non-decisional 块要求 delta 候选，且 `changed_sections` 与最近 decisional 块的 `decisional_anchors` 无交集；任何一环缺失 fail closed。全 non-decisional 链维持全量对（保守）。`

### 12.5 `SKILL.md:462`

- 原文（`:462`）：
  `> **Material revision 两-authority 同字节规则**：material revision 后须两个不同 fresh Spawn（outer + blank-slate）审查相同最终 plan 字节；任何字节变化使两份审查同时失效。详见 `refs/orchestrator-guide.md` §Material revision。`
- 替换为：
  `> **Material revision 分级复核规则**：`decisional` material revision 后须两个不同 fresh Spawn（outer + blank-slate）审查相同最终 plan 字节；任何字节变化使两份审查同时失效。`non-decisional` revision 走单 fresh delta reviewer 增量复核（delta payload + hash 链 + 交集 fail-safe），任何一环缺失 fail closed。详见 `refs/orchestrator-guide.md` §Material revision。`

---

## §13 回滚

1. 代码回滚：`git checkout -- scripts/orchest.py tests/test_loop_a_coverage.py tests/test_process_controller_contract.py`。
2. 规范回滚：`git checkout -- refs/state-schema.md refs/orchestrator-guide.md SKILL.md scripts/README.md`。
3. 语义回滚：新字段为**纯增量**，旧块缺省 decisional，回滚后旧数据无需迁移；删除 delta 相关代码即恢复现状。
4. 若已归档且需重开：按 `SKILL.md` §收敛后修订 的 `archive_convergence.py reopen` 流程，禁止手工移动。

---

## §14 附录 A：本计划的 `converge.governance-change/v1` 机器块

> 本块是本计划作为治理计划的唯一机器输入。`numeric_changes` 全部 `kind: mechanism` 且 `comparison: null`（本计划不改任何数值默认/阈值/停止条件；`comparison` 对 mechanism 必须为 `null`）。
> `user_message_events`：`execution_authorization=989e5ebd-2406-4dcf-bf77-283bcb4e6e17`（B 对象事件流 sequence 49，2026-09-12 收口裁决，见 `.converge/done/20260911-op-envelope-b-contract-correction/evidence/events/00000049-989e5ebd-2406-4dcf-bf77-283bcb4e6e17.json`）、`quality_goal=06754e6f-94fc-4a8a-9d80-65f89ae68e3a`（B 对象 sequence 10）。**两项均为 B 对象事件，本对象事件流内暂无对应事件**；preflight 仅做 UUID 格式校验（`scripts/budget_gate.py:1668-1679`），若编排层将校验升级为"本对象事件流内存在"，则改为编排层后补清单并如实披露，不伪造。
> `calibration.path` 指向 `attempts.md` 内的报告 fence；其 canonical 字节与 `evidence/calibration-report.json` 一致（canonical sha256 `16ec2455…`，`corpus_digest=3627eb4d…`），独立文件不被 preflight 机械校验（locator `root-file` 限 allowlist 普通文件名，`evidence/` 不可寻址——`refs/state-schema.md:76`）。

```json
{
  "schema": "converge.governance-change/v1",
  "change_id": "o6-material-delta",
  "numeric_changes": [
    {
      "control": "material-revision-change-class",
      "kind": "mechanism",
      "released": null,
      "old": null,
      "proposed": null,
      "comparison": null,
      "basis": "classify_material_revision_decisional_or_non_decisional"
    },
    {
      "control": "material-delta-recertification",
      "kind": "mechanism",
      "released": null,
      "old": null,
      "proposed": null,
      "comparison": null,
      "basis": "single_fresh_delta_reviewer_replaces_dual_authority_for_non_decisional"
    },
    {
      "control": "material-gate-chain-backtrack",
      "kind": "mechanism",
      "released": null,
      "old": null,
      "proposed": null,
      "comparison": null,
      "basis": "backtrack_to_last_decisional_block_then_require_delta_per_non_decisional_link"
    }
  ],
  "archaeology_refs": [
    "git:da81e70cee7931d49680866f9cd0b2cdb6fadc5a",
    "archive:done/20260911-op-envelope-b-contract-correction"
  ],
  "calibration": {
    "path": "attempts.md::json-fence[schema=converge.calibration-report/v1,id=calibration]",
    "sha256": "16ec2455acb9cdb143145e8f28c52b4db1d96f9cd90b44ab4b8af897df3303c8",
    "corpus_digest": "3627eb4d72cfbf07a3c1a6655026c9ce51063506e613c3a7df5b3519b10224ea",
    "freshness": {
      "repository_head": "da81e70cee7931d49680866f9cd0b2cdb6fadc5a",
      "source_archive_revision": "unavailable",
      "source_event_high_watermark": 0
    }
  },
  "counterevidence_refs": [],
  "user_message_events": {
    "quality_goal": "06754e6f-94fc-4a8a-9d80-65f89ae68e3a",
    "execution_authorization": "989e5ebd-2406-4dcf-bf77-283bcb4e6e17"
  }
}
```

## §15 修订历史

| 版本 | 触发 | 摘要 |
|---|---|---|
| candidate-1 | 初稿（fresh Plan Author） | 基于 B 成本样本重评 O6；D1 分级 + delta 路径 + 链回溯材料门；D2 决议协议；治理机器块 + calibration 实核 |
