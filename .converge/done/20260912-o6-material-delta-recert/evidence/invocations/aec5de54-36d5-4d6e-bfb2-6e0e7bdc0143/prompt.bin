# Task: O6 material 增量复核 · 完整收敛 outer 评议 R2（candidate-3）

你是 fresh 独立 Reviewer（独立上下文，与 plan 作者及此前评审无共享）。仓库：`C:\Users\Administrator\Documents\Github\converge-skill`。只读（本任务书指定输出除外）。UTF-8。

## 受审对象

`.converge\active\20260912-o6-material-delta-recert\plan.md`（candidate-3：R1 BLK-1..4 + NB-1..6 修复后。O6 material 修订分级 + 增量复核机制）

## 必读

1. 上述 plan.md
2. 同目录 `round-1.md`（R1 阻断/非阻断及单选修法）、`uv-init-1/2/3.md`、`attempts.md`（全部处置表 + preflight 实跑输出）
3. `CONSTITUTION.md`（:63-96）、`SKILL.md`（:157-186）、`refs/design-review-prompt.md`、`refs/orchestrator-guide.md:17-32`、`refs/state-schema.md:96-118`
4. 代码事实：`scripts/orchest.py`（:1089-1361 材料门）、`scripts/archive_contract/model.py`、`scripts/budget_gate.py`、`tests/test_loop_a_coverage.py`（TestMaterial* 17 条）

## 评审重点

0. **R1 处置完整性（最优先）**：BLK-1..4 与 NB-1..6 是否逐条按单选修法落地（yes/no + plan 原文行号）；特别：章节域语义统一（BLK-1，12 项词表 + UNION 交集）、legacy 块绕过域门（BLK-2）、legacy full-pair 范围含当前块（BLK-3，单块旧对象不 fail-open）、candidate_plan/artifact 统一（BLK-4）。
0.5 **UV 三票处置抽查**：≥8 条回 plan 原文，确认未被 candidate-3 改动破坏。
1. 前置自检 5 问 + DR 7 维。
2. **D1 分级机制可判定性**：12 项词表全篇一致；"归类争议=decisional" fail-safe；delta reviewer 职责闭环。
3. **材料门链式不变量**：块链回溯 + base/current hash 链与既有三处锚点共存；legacy 回退；全 non-decisional 保守回退。
4. **对抗面**：谎报分级、伪造 base hash、删块、verdict 造假（含 `需重新设计`）。
5. **§12 逐字对照**完整性（两处标题 + README:151 + 词表全文）。
6. **机器块**：preflight 实跑输出真实性（可复跑）；跨对象 user_message_events 引用与后补路径。
7. **D2 record-only 分支**可判定性。
8. 事实核验：行号/符号实开核对；Acceptance 条条机械可判定。

## 输出

文件：`.converge\active\20260912-o6-material-delta-recert\round-2.md`

- 首行 verdict：`可执行` / `阻断需修复` / `需重新设计`
- 前置自检 + R1 处置清单 + UV 抽查表 + 逐条 issue（编号 + severity + 文件:行 + 单选修法）
- 若 `可执行`：实施前必须满足的前置条件清单（可为空）
- 结尾：D1 方向是否成立；无法核实的断言清单

判定纪律：事实失真即阻断；穷举缺一即阻断；不为凑数升级。
