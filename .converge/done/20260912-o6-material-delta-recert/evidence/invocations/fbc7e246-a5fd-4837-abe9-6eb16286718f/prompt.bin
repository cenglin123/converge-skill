# Task: O6 material 增量复核 · 强制设计审查（ultraverge 终局）

你是本对象的设计审查者（fresh、独立上下文）。仓库：`C:\Users\Administrator\Documents\Github\converge-skill`。只读（本任务书指定输出除外）。UTF-8。

## 审查对象（终局字节，冻结）

`.converge\active\20260912-o6-material-delta-recert\plan.md`（candidate-4）

## 依据

1. `refs/design-review-prompt.md`（按其 DR 维度与输出要求执行）
2. 上述 plan.md
3. 代码：`scripts/orchest.py`（:1089-1361 材料门）、`scripts/archive_contract/model.py`、`scripts/budget_gate.py`、`tests/test_loop_a_coverage.py`

## 任务

按 `refs/design-review-prompt.md` 的维度对计划做**设计质量**独立审查（事实已由双权威复核，不重复事实核对）：

- 架构与模块边界：`change_class` 分级 + delta 复核路径 + 链式回溯，落点是否干净；有无更简单的等价设计被错过（Occam）。
- 失败语义：全部新 fail-closed 路径的错误码/退出码/披露是否一致可诊断。
- 约定与命名：新字段/payload 键/失败码与仓库既有约定是否协调。
- 系统边界：与 reopen/supersede、既有三处哈希锚点、governance preflight 的交互是否闭合；对历史归档 check 的影响。
- 向后兼容与回滚：legacy 回退、每项改动能否独立回滚。
- 可测性：§11 对抗清单是否封死滥用面（谎报分级/伪造 base/删块/verdict 造假）。
- 实施顺序：Phase 划分能否在实施中及时暴露设计错误。

## 输出

文件：`.converge\active\20260912-o6-material-delta-recert\design-review.md`

- 首行结论：`设计通过` / `设计需修订`（二选一；修订级问题必须列出）
- 按 DR 维度逐维结论（pass/issue + 理由）
- 必须在实施前修正的设计级问题清单（可为空；每条单选修法）
- highlights：3-5 条最值得肯定/最值得警惕的点
