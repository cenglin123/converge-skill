## Mode: converge post-revision outer recertification

You are the fresh outer Reviewer for a material governance-plan revision. Review independently and do not defer to any prior verdict.

## Task

Determine whether the frozen implementation plan is executable with zero conceptual, architectural, structural, or empirical blockers. In particular, verify that the bootstrap calibration locator and exact two-Reviewer evidence chain are fully specified, deterministic, and implementable without expanding Archive Contract v1.

The user quality goal is: "我原以为，通过 converge 流程，走多轮迭代式审计收敛，就可以集不同的视角来查漏补缺，得到一个相对更优的方案。但是现在看来并非如此。" The plan must provide bounded but sufficiently deep autonomous convergence, genuinely independent perspectives, and a real path for historical feedback to affect later governance decisions.

## Input

- Repository: `C:/Users/Administrator/Documents/Github/converge-skill`
- Required artifact: `C:/Users/Administrator/Documents/Github/converge-skill/.converge/active/20260910-process-controller-consolidation/plan.md`
- Material record: `C:/Users/Administrator/Documents/Github/converge-skill/.converge/active/20260910-process-controller-consolidation/attempts.md`, but read only the JSON block with id `material-r2-candidate-2`
- You may inspect `CONSTITUTION.md`, `SKILL.md`, relevant `refs/`, `scripts/`, `tests/`, the current Git diff, and commits `aac95bd`, `0137fce`, `20c8993`, and `d3c82cb` as read-only evidence.
- Other `.converge/` files are forbidden, including prior review products, prompts, retrospective, event files, and invocation blobs. Do not cite or infer prior verdicts.

Before reviewing, recompute the artifact SHA-256 and byte size, resolve the named material block using the locator below, canonicalize it as sorted-key compact JSON plus one LF, and recompute its SHA-256. Fail closed on any mismatch.

```json
{"artifact":{"path":"plan.md","sha256":"ef2e844130a62e33ab8d33c92179f2becabcc27b3c9b929f6c5c3f1d1c3487cb","size":34302},"material_revision":{"locator":"attempts.md::json-fence[schema=converge.material-revision/v1,id=material-r2-candidate-2]","sha256":"c99f4dc69c62604c1df696bfd2a1349195327541c927e176ec6a734c36f69e90"},"quality_goal_event_id":"bdd405f3-2b03-40eb-9db2-09a32afacae2","revision_id":"r2","schema":"converge.review-target/v1","target_id":"r2-plan"}
```

## Output

- Write only `C:/Users/Administrator/Documents/Github/converge-skill/.converge/active/20260910-process-controller-consolidation/round-3.md`.
- The file will already contain YAML frontmatter binding the invocation. Preserve that frontmatter exactly and replace only the skeleton body.
- Include a `reads:` list of every absolute path actually read and a `commands:` list with each read-only command and exit code.
- Include exactly one fenced JSON block whose single payload line is byte-identical to the review-target line above.
- Include one fenced YAML verdict block with `reviewed_plan_sha256`, `reviewed_plan_size`, `verdict` (`可执行|阻断需修复|需重新设计`), `blocking_issues`, `suggestion_issues`, and explicit checks for locator closure, exact review-evidence closure, historical evidence, empirical-conflict gate, task-envelope honesty, compatibility/Occam, and user-goal alignment.
- `可执行` requires `blocking_issues: []`. Do not hide a blocker as a suggestion.

## Boundaries

Do not modify the plan, attempts, repository source, tests, Git state, or any path other than the one output file. Runtime checks must be read-only and must not generate caches or formatting changes. If a fact is unavailable, report it rather than guessing.

## Execution Evidence

Your final response must report the output path, byte size, and tools/commands used. Writing anywhere else invalidates the review.
