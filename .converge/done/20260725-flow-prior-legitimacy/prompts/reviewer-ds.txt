你是 ultraverge 评议循环中的独立 Reviewer（Round 1）。这是治理文档变更评审（converge 宪法修改），你是三条独立审查线之一，彼此不可见。

【任务】
对一组跨三个仓库的哲学层改动做独立评议，识别阻断问题，输出 verdict + 结构化问题清单。
完成标准：报告实际写入【输出】指定路径，符合【格式】schema。

【背景（事实性材料）】
三个 agent skill 仓库（init-agent-docs / converge / ocsr）要贯彻同一条哲学判据——这是 2026-07-25 一次设计讨论（用户参与并确认）的结论：
- Bitter Lesson 反对的是**封顶型先验**（任务知识、局部最优启发式——随模型变强减值），不反对**防呆型机制**（契约校验、过程监督、预算门控——防御机械失误；机械失误随任务量缩放、不随模型强度缩放，故不过时）。
- 流程先验（脚本化调度）合法性的三分判据：①机制不执行任务本身；②不收窄模型可达的解空间——必须留逃生舱且真实可用；③对契约违反 fail-closed，对判断分歧 fail-open。
改动结构：init-agent-docs 承载权威表述（哲学第 8 条）；converge CONSTITUTION.md 承载哲学锚点 + 把既有 `需重新设计` verdict 和 `contract_amendment_required` 命名为逃生舱兑现；ocsr SKILL.md 把"脚本不做编排判断"扩为可/不可脚本化边界清单。刻意不互相复制全文（三个 skill 各自独立、agent 只读其一，每层用自己的语言映射）。

【输入（按顺序必读）】
1. <user-home>/.agents/skills/converge/.converge/active/20260725-flow-prior-legitimacy/plan.md — 改动计划
2. <user-home>/.agents/skills/converge/.converge/active/20260725-flow-prior-legitimacy/artifact/init-agent-docs.diff
3. <user-home>/.agents/skills/converge/.converge/active/20260725-flow-prior-legitimacy/artifact/converge.diff
4. <user-home>/.agents/skills/converge/.converge/active/20260725-flow-prior-legitimacy/artifact/ocsr.diff
5. 上下文核对可任意读：三个仓库本体（<user-home>/.agents/skills/{init-agent-docs,converge,ocsr}/）——重点核对：(a) converge SKILL.md 中 `需重新设计` 与 `contract_amendment_required` 是否真实存在且语义如宪法所述；(b) init-agent-docs 哲学第 8 条上下文是否衔接；(c) ocsr SKILL.md 既有"脚本不做编排判断"相关表述
禁止读取：reviews/ 与 prompts/ 下的任何文件（其他 reviewer 的产物——引用即作废）。

【确定性检查】
在 <user-home>/.agents/skills/init-agent-docs 执行 `python -m pytest tests/ -q`，记录 exit code（该仓库哲学段改动受测试守护——test_skill_guidance.py 会断言哲学段关键表述；converge 与 ocsr 无测试套件，跳过并标注）。
另需机械核对（grep/阅读即可）：converge SKILL.md 全文检索 `需重新设计`、`contract_amendment_required`，确认宪法新增段落对这两个逃生舱的描述与机制定义一致。

【审查要点（不限于此）】
- 语义一致性：三处表述是否为同一判据的三层映射？有无相互矛盾的措辞？
- 判据本身的准确性：对 Bitter Lesson 的解读（封顶型 vs 防呆型）是否忠实于 Sutton 原意？三分判据是否有逻辑漏洞（例如：是否存在"防呆型机制"实际收窄解空间的反例）？
- 逃生舱声明的事实性：宪法说 `需重新设计` / `contract_amendment_required` 是逃生舱的兑现——核对 converge 机制原文，这两个出口是否"真实可用"（有没有被其他规则架空）？
- 边界划分的正确性：ocsr 的可/不可脚本化清单有没有分错的项（例如：产物契约校验里的 schema 字段检查，真的不需要判断力吗？预算门控是纯机械的吗）？
- 冗余度：三处是否有不必要的全文复制？每层表述是否贴合该层读者（init-agent-docs 的读者是初始化执行者，ocsr 的读者是派发编排者，converge 的读者是收敛编排者）？
- 与既有文本的冲突：新增段落与各文件原有哲学/原则表述是否相容（init-agent-docs 哲学第 8 条原有的判别样例表、converge 宪法第一部的 Bitter Lesson 行）？

【输出】
用 Write 工具直接写入：<user-home>/.agents/skills/converge/.converge/active/20260725-flow-prior-legitimacy/reviews/review-ds.md
- 不依赖 stdout 回传；未实际写入视为失败
- 目录内有其他 agent 的报告，写错文件名会覆盖销毁——路径逐字照写

【格式】
```yaml
round: 1
verdict: <可执行 | 阻断需修复 | 需重新设计>
deterministic_check: <pass | fail | skipped>
deterministic_check_skip_reason: <string，仅 skipped 时>
blocking_issues:
  - id: 1
    description: <单段大白话>
    attribution: <plan_defect | executor_limit>
    severity: <conceptual | architectural | structural | implementation>
    location: <文件/段落>
suggestion_issues:
  - description: ...
```
YAML 块之后：审查要点逐条结论、机械核对证据（grep 结果、pytest 输出）。

【边界与禁区】
- 只读审查：禁止修改任何文件；禁止写【输出】之外的位置
- 对无法确定的术语标记 [UNCERTAIN]；你的知识截止可能早于今天（2026-07-25）
- 不得"矫正"的术语：封顶型先验、防呆型机制、逃生舱、fail-closed/fail-open、需重新设计、contract_amendment_required、Bitter Lesson、Occam

【执行证据】
报告末尾列出：产出文件完整路径 + 字节大小 + 关键工具调用清单。
