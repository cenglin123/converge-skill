diff --git a/CONSTITUTION.md b/CONSTITUTION.md
index b9c4c03..3ccd838 100644
--- a/CONSTITUTION.md
+++ b/CONSTITUTION.md
@@ -28,6 +28,8 @@
 | **Occam** | 这东西解决什么具体问题？不解决具体问题的实体删除，git log 是考古层 | 三层物理分离（不让会变化的知识腐化在不变的机制里）；章程瘦身（引用表替代重复副本） |
 
 > 新增文件/规则/字段时，两条原则同时适用：先问 Bitter Lesson（硬编码还是 compiled？），再问 Occam（必要还是多余？）。
+>
+> **Bitter Lesson 的精确边界（流程先验合法性判据）**：反对封顶型先验（任务知识、局部最优启发式——随模型变强而减值），不反对防呆型机制（契约校验、过程监督、预算门控——防御机械失误；机械失误随任务量缩放、不随模型强度减值）。流程先验（如脚本化调度本 SKILL 的状态机）合法的三条判据：①机制不执行任务本身；②不收窄模型可达的解空间——必须留逃生舱且真实可用，本 SKILL 的兑现：`需重新设计` verdict 退出修复循环、`contract_amendment_required` 允许模型修合同本身（连续使用会触发意图漂移审计，但不阻止修改）；③对契约违反 fail-closed，对判断分歧 fail-open。budget_gate / archive contract / ledger 属防呆型机制；verdict 裁决、prompt 工程、重试 prompt 修订、机制阈值与参数的设定永远留在 Orchestrator 判断侧。
 
 ---
 
