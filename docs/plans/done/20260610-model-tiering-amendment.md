# Plan: converge 模型分层机制（小型修宪，20260610）

> **性质**：修改 SKILL.md（治理文档）→ 按 CONSTITUTION 第四部走 ultraverge；同时新建 `refs/model-tiers.md`（按第三部规则默认不受宪法保护）。
> **范式声明**：本计划试行 20260610 设计审查建议的**轻量计划范式**（动机一段 + 改动清单 + 验收清单，不铺 F/C/§五 三层）——适用条件：单一动机、纯新增、不触碰既有语义。本轮评议新增两条适用规则：(i) 声明低成本执行的计划**必须附逐字插入块**；(ii) 修改对象为治理文档时**必须附入边引用盘点结果**（本计划补附：见「入边盘点」节）。
> **执行注记**：落地定位一律以各改动项的引文锚点为准（锚点字符串已逐一 grep 核实为 SKILL.md 内唯一）。各插入块为逐字合同，执行即原样插入；据此本计划可由低成本 agent 执行。

## 动机（即唯一发现）

实证（20260610 减重计划执行）：低成本 agent 执行确定化合同质量合格，全部偏差出现在合同歧义处而非模型能力短板处。结论：**执行质量的真正变量是合同确定性，不是执行模型强弱**（验证不对称性：审查/验收需要判断力，确定化合同的执行接近机械转写）。机制层应将"Executor 角色可降档至低档"的许可条件固化进 converge；具体型号映射是易腐数据（实证：本日对话中用户与 Orchestrator 先后各出现一次型号知识过期），外置到非保护 ref。

## 入边盘点（治理文档修改的引用核对）

1. **附录 A.3 codex 约束 #4「模型继承优先」**——与新增「模型分层」小节在 Executor 模型档位选择场景存在管辖重叠（#4 含第三条许可路径"scope 有清晰的任务级理由"，无需用户授权，故新小节是对它的**收紧**而非同义复述）。管辖切分已在 M1 插入文本中显式声明：档位选择场景以新小节为准，A.3 #4 继续管辖其余 model override 情形。
2. **既有"降级"词域**（ultraverge 降级路径、A.2/A.3/A.4 能力与流程降级、必检"若触发了降级"项、宪法第二部 #6/#7）——本计划全程改用**降档**词系（降档/档位/低档）隔离，M1 插入文本含语义切割句，二者互不触发对方义务。
3. **其余 refs/**——已 grep「模型|model」核实：除 SKILL.md A.3 #4 外，全仓库无模型选择/override 的规范性表述（命中仅为"收口模型的综合判断"、Bitter Lesson 自检措辞等无关用法），本计划不产生其他入边冲突。

## 改动清单（即合同）

### M1 · SKILL.md 新增「模型分层」小节

**锚点**：在「核心角色」表最后一行「`| **Executor** | 每轮 Spawn 的独立 agent | 全新上下文；prompt 自足 |`」与其后的「`---`」之间插入（与上下各空一行），标题层级 `###`。逐字插入：

```markdown
### 模型分层

Executor 可降档（模型档位下调）至该家族低档执行，**仅当同时满足以下三条件**：

- (a) 执行合同已通过确定性检查——指令含逐字文本或等价的零歧义规格；
- (b) 用户显式授权或计划明示——默认 inherit（继承主对话模型），**不静默降档**。本条**收紧并跨框架推广**附录 A.3 codex 约束 #4（模型继承优先）：Executor 模型档位选择场景中二者冲突时以本节为准，A.3 #4 继续管辖其余 model override 情形；
- (c) 降档时验收**必须**包含确定性核对（diff / grep / 测试 / 计数），不得仅语义验收。

**不降档角色**：Reviewer、Orchestrator、设计审查（判断力密集）。确定性核对类子任务（清点、diff/grep 核对、行数统计——判别准绳：任务存在客观判准、其结果可被机械复核）可用低档。未列出的角色（如层级模式的 Worker、仲裁 agent、L2 gate Reviewer）默认 inherit。

档位经由各框架 Spawn 的模型选择参数实现（如 Claude Code Agent 工具的 `model` 参数）；框架不支持模型选择时视同 inherit。降档（模型档位下调）与本 SKILL 既有的"降级"（能力/流程降级，见附录 A.4）语义无关，互不触发对方的义务。档位取值与三条件核对结果须记入 attempt log。

家族档位对照见 `refs/model-tiers.md`（数据层，随模型换代更新，无需修宪）。
```

### M2 · SKILL.md「配置参数」表新增一行

**锚点**：在「配置参数」表当前最后一行「`| `ultraverge_min_reviewers` | 3 | ultraverge 评议阶段最少并行 Reviewer 数（默认 3，来自 ≥3 自动收敛阈值。可随实证数据调整） |`」之后追加。逐字插入：

```markdown
| `executor_model_tier` | `inherit` | Executor 模型档位。`inherit` = 继承主对话模型；`low` = 该家族低档（对照表见 `refs/model-tiers.md`）。仅当「模型分层」小节三条件满足时可设 `low`。初始策略，随实证数据调整 |
```

### M3 · SKILL.md「拆分文件索引」表新增一行

**锚点**：在「拆分文件索引」表当前最后一行「`| Reviewer/Executor/Orchestrator 反模式巡查清单（动态注入，status 由 distill 维护） | `refs/antipatterns.md` |`」之后追加。逐字插入：

```markdown
| Executor 降档时选择各家族低档 | `refs/model-tiers.md` |
```

### M4 · 新建 `refs/model-tiers.md`（数据层，非保护）

**锚点**：新文件，全文逐字如下：

```markdown
# Model Tiers — 家族档位对照表

> 本文件为数据层、非治理文档，对 Agent 行为无规范性约束力（规范在 SKILL.md「模型分层」小节）；更新无需修宪程序，每次更新注明核实日期与核实方式（联网核实，不依赖任何 agent 的记忆）。
> 强档列为参考信息（判断密集角色当期主力档）；Orchestrator 模型由用户会话决定，converge 无权选择。
> 核实日期与方式：Claude 行 Fable 5 由运行时会话佐证（2026-06-10）；OpenAI / Gemini / DeepSeek 三家族为联网核实（2026-06-10）。

| 家族 | 低档（Executor 降档目标） | 强档（参考：判断密集角色当期主力档） |
|------|--------------------------|--------------------------------------|
| Claude | Haiku 4.5（备注：Sonnet 4.6 为介于两档之间的备选，不构成独立档位） | Fable 5、Opus 4.8 |
| OpenAI GPT | GPT-5.4 mini、GPT-5.4 nano | GPT-5.5、GPT-5.4 |
| Gemini | Gemini 3.5 Flash、Gemini 3.1 Flash-Lite | Gemini 3.1 Pro |
| DeepSeek | DeepSeek-V4-Flash | DeepSeek-V4-Pro |

使用规则：

1. 档位是家族内相对概念。单元格列出多个型号时，取该家族当期最便宜的通用档。
2. 过期兜底：表过期时以该家族当期定价最低的通用档为准，不阻塞执行。
```

### M5 · SKILL.md「收敛完成前必检」追加一项

**锚点**：在「收敛完成前必检」清单当前最后一项「`- [ ] 若触发了设计审查：`design-review.md` 已写入，highlights 已报告用户，用户决策已记录`」之后追加。逐字插入：

```markdown
- [ ] 若本次收敛中 Executor 使用了降档（low）：验收已包含确定性核对，档位取值与三条件核对结果已记入 attempt log
```

## 范围声明

- 须 ultraverge：SKILL.md（M1/M2/M3/M5，纯新增，不改写任何既有行）
- 非保护新建：refs/model-tiers.md（M4）
- 明确不动：CONSTITUTION.md、其余全部 refs/、scripts/、README
- **原子性**：M1-M5 一次提交；任一验收项失败则整体不提交、计划退回修订
- 行数：本修宪为机制新增，**不适用**减重计划的行数容差条款；预计 SKILL.md +16~18 行

## 验收清单

1. M1 小节位于「核心角色」表与其后的 `---` 之间，标题层级 `###`；M1 文本含「收紧并跨框架推广」表述与管辖切分语句（逐字对照 M1 插入块）。
2. M2 行在「配置参数」表 `ultraverge_min_reviewers` 行之后，逐字等于 M2 插入块（含默认值 `inherit`、「仅当「模型分层」小节三条件满足时可设 `low`」与「初始策略」标注）。
3. M3 行在「拆分文件索引」表最后一行之后，逐字等于 M3 插入块。
4. refs/model-tiers.md 存在且全文逐字等于 M4 块（含数据层/免程序声明、核实日期与方式、四家族两档对照表、多型号取最便宜通用档规则、过期兜底规则）；且**不**出现在 CONSTITUTION 第三部保护清单（无需改宪法，验证即可）。
5. M5 项位于「收敛完成前必检」清单末尾，逐字等于 M5 插入块。
6. SKILL.md diff 仅含 M1/M2/M3/M5 新增行 + M4 新文件，零既有行改写/删除（git diff 逐行验证）。
7. 词域核对：M1-M5 插入文本 grep 零命中「盲评」「最低档」「中档」；「降级」仅出现在 M1 语义切割句对既有术语的引用处。
8. 双原则自检：Bitter Lesson——许可条件（机制）硬编码、型号映射（数据）外置 compiled；Occam——解决"多 agent 编排的执行成本"具体问题，每处新增对应 M1-M5。
9. 责任清单（SKILL.md 现四分组结构）**不新增条目**——模型档位决策归入既有 #1（Spawn 真实性）与 Spawn 行为本身，不扩表（防清单再膨胀）。

## 状态

- [x] 计划写入（本文件）— 2026-06-10
- [x] ultraverge：≥3 并行 Reviewer 评议（R1=6 联合阻断）
- [x] 完整收敛（R2 fresh reviewer 可执行零阻断，字节级核验）
- [x] 强制设计审查（3 highlights，见 .converge/done/20260610-model-tiering-amendment/design-review.md）
- [x] 执行 + 验收（执行由用户于 22:12 自行提交 b76b156；R2 转事后字节级验收通过——时序倒置如实记录于 retrospective §1）
- [x] 人工确认提交（commit b76b156/b82a227，作者 CL）
