# 等价映射对照表 — 文档层 need-to-know 削减（DELTA）

持久化《docs/plans/active/20260826-doc-need-to-know.md》验收 #2 的等价映射对照表。
本表为**本 delta 自有**等价映射表，与前序已单源条目（《20260826-doc-layer-refactor.md》及其归档映射表 `docs/plans/done/20260826-doc-layer-refactor-equivalence.md`）区分：前序条目本 delta 不重做、不在本表重复登记。
> 注：M-11 hook-wiring 与 §六·预算追踪 + gate 编排 命令序列的等价映射已在前序归档映射表登记（rows 16/21），属**前序已单源、本 delta 不重做**，故本表**不重复登记**为删除行。quality-gate.md L82 指针重定向已折叠进 row 5（不新增行，保持 8 行计数）。

| # | 主题 | 源位置（改动前） | 单源/目标位置（改动后） | 等价性判定 |
|---|------|------------------|------------------------|------------|
| 1 | `max_total_reserved_spawns` 数值复算移除 | SKILL.md：[^totalcap] 脚注**定义与推导复算**（`ceil(1.5*42/41)`、`63/62` stock 双档表、无边界下溢说明等）；Ultraverge-path（原 L168）；配置区 NOTE（原 L454）「total 上限按规范式重算——普通=63 -> ultraverge=62」 | `scripts/budget_gate.py` DEFAULTS + 规范公式（SKILL.md 保留公式语句与 8 键 DEFAULTS 指针）；一行行为事实（普通=63 / ultraverge=62）保留于 SKILL.md | 等价：删除重复/陈旧推导式复算；规范式本体与 DEFAULTS 指针保留；普通=63 / ultraverge=62 作为行为事实一句保留，不展开推导。**联动**：删除 [^totalcap] 脚注定义后，SKILL.md 配置表行（`max_total_reserved_spawns`）中的 [^totalcap] 内联引用标记一并删除，该行仅保留一行行为事实 |
| 2 | 预算执行哲学注压缩 | SKILL.md 预算执行哲学注（原 L456 六行机制复述：file-authoritative gate 核心、不依赖 Orchestrator 记忆计数等） | `scripts/budget_gate.py`（每次 spawn 前裁决）+ framework-adapters 分册（§A.1 claude-code / §A.6 kimi-code / §A.7 dsh，trust boundary 三级能力）；「不得靠记忆计数」行为禁令**规范式保留于 `refs/orchestrator-guide.md` §六（不靠 Orchestrator 记忆比较计数）**；SKILL.md 压缩注仅指针 | 等价：压缩为一句「预算由 `budget_gate.py` 在每次 spawn 前裁决；trust boundary 三级，逐级能力见 framework-adapters 分册（§A.1 claude-code / §A.6 kimi-code / §A.7 dsh）；[行为禁令「不得靠记忆计数」规范落在 refs/orchestrator-guide.md §六;SKILL 此处仅指针]」；机制复述删除，行为禁令**规范式保留于 guide §六**，SKILL.md 压缩注仅指针且指针不丢失 |
| 3 | 必检清单 2 项机械项移除 | SKILL.md 必检清单：「`archive` 返回成功」（finish 的 landing 动作，非 agent 义务）、「done 最终路径只读 `check` 返回 valid-v1」（finish 步骤 8） | `scripts/orchest.py finish`（机械保证）+ `archive_convergence.py check`；保留一行提醒：extension 关联 BLOCK decision + round-stamped 用户原话、每 spawn 有 reservation 且 settle / 无孤儿、extension 仅抬 ceiling、auditable-only 无机械兜底 | 等价：删除脚本已机械保证、agent 无需确认的项；语义/判断提醒（含 user_quote 授权判断、混合后端逃逸、extension 仅抬 ceiling、auditable-only 纯 prose）全部保留为一行 |
| 4 | 主循环内部实现描述收缩 | SKILL.md 主循环内部实现（gate reserve + begin-invocation + 骨架落盘、settle 语义内嵌、begin kind=continue 计数入 max_inner_loops） | `scripts/README.md`（命令名+用途）+ `scripts/orchest.py`（内部实现） | 等价：agent 只需命令名+用途+指针；**保留**语义判断点（★步骤）与盲审不变量（pass|fail|waived、共享 max_outer_loops、不自动扩），并**保留内环预算不变量「continue 不推进 max_outer_loops、不占新 spawn cap（仅计数入 max_inner_loops）」**（来源：前序归档映射表 `docs/plans/done/20260826-doc-layer-refactor-equivalence.md` row 8）——该不变量随 ★/盲审不变量**保留在 SKILL.md 主循环**，**不**仅靠 `scripts/README.md` L82 指针（L82 只写「计数入 max_inner_loops=3」，未写「不推进 max_outer_loops / 不占新 spawn cap」，指针不足单独承接） |
| 5 | state-schema §预算 gate 缩减（NARROW：L79 三类；含 guide §六 全量数据契约引用改指 + quality-gate L82 指针重定向，折叠） | `refs/state-schema.md` §预算 gate（约 L378-L502）维护者视角大段中**仅 PURE MACHINE JSON schema 重复**（L79 三类：`gate-ledger.jsonl` 精确 JSON 字段规格、`_budget-state.json` 内部字段、计数模型内部结构；即 gate-ledger 各事件类型完整契约、`_budget-state.json` JSON 字段与计数模型/双计数模型内部结构全文）；`refs/orchestrator-guide.md` §六·预算追踪 + gate 编排 对「全量数据契约」的引用（原「数据契约见 refs/state-schema.md §预算 gate」）；`refs/quality-gate.md` L82 引用（原「完整角色对照表见 refs/state-schema.md §预算 gate『角色对照表』」）。**NARROW / 保留**：extension 校验细节（L437，含 `user_quote` 人类可审计凭据、非机械可证明）与 task-envelope 四档档位表（L461-466）**不在删除范围、保留**（agent-relevant/判断） | 单一权威源 = `scripts/budget_gate.py`（编译：`ROLE_CONSUMES`、公式、事件/schema、计数模型）；state-schema 保留命名锚点 `角色对照表`（缩减为 agent-relevant「角色→consumes」摘要：哪些角色 consumes outer/blind/ultraverge/none；仅删 `终局-owner-资格` 列）+ `任务档预算`/task-envelope scope 语义指针与**档位表（L461-466）** + **extension 校验细节（L437，含 `user_quote` 人类可审计凭据，非机械可证明）** + 一句「machine schema / `ROLE_CONSUMES` / 计数模型单一权威源 = `scripts/budget_gate.py`」；guide §六 改为「全量机器数据契约以 `scripts/budget_gate.py` 为单一权威源（编译）；state-schema §预算 gate 仅保留 agent 需读的角色摘要」；quality-gate L82 改为「consumes-summary 在 state-schema §预算 gate『角色对照表』；完整 `ROLE_CONSUMES` 单一权威源 = `scripts/budget_gate.py`」；L506+ §结构化协议字段扩展（evidence_tier/dispute_topic_id）不动 | 等价：只删 PURE MACHINE JSON schema 重复（L79 三类）、脚本仍是权威；`角色对照表` 锚点保留（不重命名），agent 仍能读到「角色→consumes」摘要；**extension 校验细节（L437，含 `user_quote` 人类可审计凭据、非机械可证明）与 task-envelope 档位表（L461-466）为 agent-relevant/判断，故保留、不在删除范围**；`终局-owner-资格` 列删除后由 `scripts/archive_contract/model.py`（`REVIEWER_AUTHORITIES` 枚举）+ `refs/state-schema.md` L44（`validate_reviewer_verdict_authority()` owner 授权）覆盖——**修正**：非 reviewer-discipline（无该枚举）或 quality-gate L82（已不再指向完整 owner 列）；§结构化协议字段扩展必须保留；quality-gate L82 指针重定向经修复后仍解析 |
| 6 | §六·预算追踪 + gate 编排 budget_extension 作者 schema **执行项：补齐到 guide §六**（非删除行；作者基准，非纯 gate 校验） | guide §六·预算追踪 + gate 编排 字段级 schema 需**补入**：`extension_id` / `ts` / `granted_at_usage`（连同既有 `triggering_block_event_id` / `new_ceiling` 单调递增且 `prior_ceiling` 接上链 / `supersedes` / `scope`|`observed_usage`|`effective_ceiling` 逐项一致 / `user_quote`） | 补齐至 guide §六·预算追踪 + gate 编排（作者基准，非纯 gate 校验）；`granted_at_usage` 由 `budget_gate.py` `validate_extensions` L383 机械校验 `== decision.observed_usage`（orchestrator 照实填写）；`extension_id`/`ts` 为纯作者字段（脚本不生成）；`user_quote` 为作者基准（`budget_gate.py` 的 `validate_extensions` 不含它），**不**指向 `budget_gate.py` 作为 `user_quote` 作者单源 | 等价：字段级 schema 为 orchestrator manual-fallback 手写的作者基准，**补齐字段描述**（非仅保留确认）；保留「关联真实 BLOCK decision + user_quote + 单调 ceiling 链；FAIL_CLOSED；不得记忆续费」 |
| 7 | orchestrator-guide §一 机械探测项「展开」收缩 | guide §一 spawn 前自检 中「若代码项目查 testing-toolbox 定 test/lint 命令」的机械展开 | `refs/testing-toolbox.md`（test/lint 命令单源） | 等价：删除机械探测展开；单源 = `refs/testing-toolbox.md`；语义自足项（prompt 自足、路径有效性、未暗示结果、升级复查、instance_id 记录）保留 |
| 8 | SKILL.md 任务级总信封 note | SKILL.md 任务级总信封 note（原 L458）机制细节复述（BLOCK 语义 / 与 total 正交 / summary 命令） | `scripts/budget_gate.py`（task-envelope 单一权威源）+ state-schema §预算 gate 角色摘要 | 等价：移除机制复述；重定向 = `scripts/budget_gate.py`（task-envelope 单一权威源）；保留正交性语义指针（与 total 正交的独立维度存在性说明） |


---

> 后续若本 delta 追加删除行，应在本表追加行，作为验收 #2 的持久化记录。
