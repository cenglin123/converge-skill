# Plan: converge SKILL 减重与修正（20260610）

> **性质**：本计划修改对象包含治理文档（SKILL.md、CONSTITUTION.md），按 CONSTITUTION.md 第四部修改程序，
> 本文件是程序第 1 步的计划文件；第 2 步须走 ultraverge（≥3 Reviewer + 收敛 + 设计审查），收敛通过后人工确认提交。
>
> **来源**：2026-06-10 对本 SKILL 的外部独立评审（Claude Code 主对话 agent 通读 SKILL.md、CONSTITUTION.md、
> refs/reviewer-prompt.md、refs/antipatterns.md 及目录结构后产出）。

---

## 一、评审结论摘要

**总评**：设计思想一流（fresh-context 对抗审查、振荡检测、降级路径、compiled 反模式注册表均为正确分层），
主要风险是**自重**——对 Orchestrator 的清单遵循要求接近人类流程管理的严格度，而 LLM 的指令遵循随上下文负载衰减。

**已核实无误的声明**（评审过程中曾质疑、后经查证确认正确，记录在案避免后续重复质疑）：

- SKILL.md 附录 A.1 关于 `/goal` 命令的描述准确：Claude Code v2.1.139 新增 `/goal`
  （changelog 原文："Added `/goal` command: set a completion condition and Claude keeps working
  across turns until it's met"）。评估器为小模型（默认 Haiku）对 transcript 的自检，
  不具备独立全新上下文——SKILL 中"不能替代独立 Reviewer"的判断同样准确。
  来源：anthropics/claude-code CHANGELOG.md；code.claude.com/docs/en/goal。

---

## 二、发现清单（按优先级）

### F1 · Orchestrator 责任清单过长，遵循概率随长度衰减 〔severity: structural〕

- 证据：SKILL.md:229-246 责任清单 18 条（编号条目）；SKILL.md:258-269 收敛完成前必检 12 条（checkbox 条目）。
- 问题：两份平铺长清单未区分"每轮必做 / 条件触发 / 收口必做"。LLM 对超长清单按概率部分遵循，
  且本 SKILL 的可靠性恰恰依赖 Orchestrator 逐条执行。CONSTITUTION.md 第二部自己也承认
  "它不会承认自己在违规"——清单越长，静默跳过越难被察觉。
- 风险：核心约束（如 overturn 判定、降级告知）与低频条目（如门控 L1）混排，稀释注意力。

### F2 · ultraverge 定义四处重复，存在漂移风险 〔severity: structural〕

- 证据：frontmatter description（SKILL.md:3）、Positioning 图（SKILL.md:19-23）、
  模式边界注记（SKILL.md:116）、执行流程专节（SKILL.md:124-150）。
- 问题：同一机制的触发条件 / Reviewer 数量 / 裁决规则在四处以不同详略重述。
  违反 CONSTITUTION 第一部 Occam 原则的"引用表替代重复副本"实践。
- 注：执行流程专节（124-150）是权威定义；其余三处中，Positioning 图做最小化改写（见 C2），模式边界注记降为一句话级指针；
  **frontmatter 例外**（见 C2）——它是技能触发元数据，须保持自足，不降为指针。

### F3 · "收敛方式"与"收敛判定 D11"两表内容重叠 〔severity: structural〕

- 证据：SKILL.md:62-68（收敛方式表）与 SKILL.md:72-80（D11 三选一表）。
  "收敛"行与 D11-a 判据逐字近似；预算软停与 D11-c 语义交叉。
- 问题：两套入口描述同一组终止状态，读者需自行对齐"方式"与"判定类型"的映射。

### F4 · 主循环编号重复及同类格式缺陷 〔severity: implementation〕

- 证据（三项同类格式缺陷）：
  1. SKILL.md:185-186 出现两个 `f.` 步骤（Spawn executor / Executor 更新 attempts.md）；
  2. SKILL.md:223 "建议在产品涉及系统级设计时启用" 中 "产品" 为 "产物" 之误（错字）；
  3. SKILL.md:152 `###` 标题中混入正文（"### 默认入口：评议。 首次审查一律……"，标题与正文融合）。
- 修复：第 1 项重排为 f/g/h/i；第 2 项改错字；第 3 项拆分标题与正文。均为纯格式修正，
  随本计划其他 SKILL.md 改动一并提交（落地细节见 C4）。

### F5 · CONSTITUTION 与 SKILL.md 的优先级表述易误读 〔severity: implementation〕

- 证据：CONSTITUTION.md:3 自称"最高治理文档"，CONSTITUTION.md:6 又规定冲突时 SKILL.md 优先。
  另：SKILL.md:33-34 存在该优先级表述的逐字镜像（"CONSTITUTION.md 是本 SKILL 的最高治理文档。
  冲突时以本文件（SKILL.md）为准"），同一表面矛盾在两份文档中各出现一次。
- 问题：解释（"运行时依赖 > 设计参考"）成立，但"最高"与"冲突时让位"并列，首次读者易困惑。
  且任何只改其中一处的修复都会制造两文档间新的措辞漂移。
- 修复方向：将"最高治理文档"改为"最高设计治理文档"或在同句内并置让位规则，消除表面矛盾，
  并**同步改写 SKILL.md:33-34 镜像句**保持两处一致。本条为措辞澄清，不改变任何语义。

### F6 · 自演化机制尚未启动，compiled 产物目前纯重量零收益 〔severity: 运维项，非文档缺陷〕

- 证据（截至计划撰写时，以 distill 元数据为唯一证据）：refs/antipatterns.md:3 `last_distilled_at: ""`；
  全部条目 `confirmed_count: 0`。注：`.converge/done/` 等目录计数随使用增长，不作为证据。
- 问题：distill 循环（retrospective → 注册表 status 维护）设计完整但从未运行，
  status 衰减、new: 前缀固化提示等机制均无实证数据。
- 处置：非文档修改，列入第四节运维行动。

### F7 · 治理修改程序摩擦力高，可能诱发绕过 〔severity: 观察项，本计划不动〕

- 证据：CONSTITUTION.md 第四部——任何治理文档修改须 ultraverge（≥3 Reviewer + 收敛 + 设计审查 + 人工确认）。
- 观察：对个人维护的 SKILL，程序成本可能导致"该改不改"或"绕过程序改"（后者使治理形同虚设）。
- 处置：**本计划不提议修改修宪程序**（修改它本身就需 ultraverge，且涉及治理哲学，应由用户单独决策）。
  仅建议：本计划将 F1-F5 全部 SKILL.md/CONSTITUTION.md 改动**打包为一次 ultraverge**，摊薄程序成本；
  执行本计划时如实记录**重量比**（计划总行数 vs SKILL.md 净减行数）与**各轮审查的约束增量**到执行
  retrospective，作为修宪程序成本的**中性数据**（不预设"可负担"或"不可负担"结论），
  供后续修宪范式决策参考。
  另须承认：本计划自身（修订后约 260 行，权威值以执行时实测为准）与其削减目标（净减 ≥10 行）的重量比已说明纯格式类修宪按本范式
  执行成本偏高，未来可考虑为"纯格式类"修宪定义更轻的计划范式（仅映射表 + 验收清单）——
  此为后续治理议题，不在本计划范围。

---

## 三、改动方案

> **执行注记**：全部 `文件:行号` 以改动前基线快照为准；落地时以引文锚点重定位
> （建议自底向上应用改动以减少行号漂移）。

### C1（对应 F1）：责任清单四分组（按执行时机，含 18 条完整映射表）

将 SKILL.md "Orchestrator 责任清单"（SKILL.md:229-246）的 18 条编号条目按执行时机重组为四组。
**零删除、零语义变更、零外部条目混入**：每条原编号条目在且仅在一组中出现一次，四组合计恰为 18 条。
下表为重组的唯一权威映射（Reviewer 验收时据此逐条 diff）：

| 分组 | 原编号条目 | 条数 |
|------|-----------|------|
| **Round 0 前置** | #11 合同谈判编排、#12 Rubrics 维度选择 | 2 |
| **每轮必做** | #1 Spawn 真实性、#2 overturn 判定、#3 Type O 计数、#4 Type R/F 等价标注、#8 instance_id/Continue 调度、#9 _orchestrator-state.md 维护 | 6 |
| **条件触发** | #5 plan_amendment_required、#6 plan 漂移检测、#7 预算追踪、#10 字段名映射、#13 contract_amendment_required、#14 收敛后修订评估、#15 门控 L1 执行、#16 门控 L2 触发决策、#17 门控发现处置 | 9 |
| **收口必做** | #18 设计审查触发与报告 | 1 |

合计 2 + 6 + 9 + 1 = 18。分组依据：#11/#12 仅在 Round 0 合同谈判阶段执行（事前前置动作，
既非每轮也非收口）；#18 在收敛宣告阶段执行；#5/#6/#7/#10/#13-#17 各有触发条件；其余 6 条每轮无条件执行。
落地改写 SKILL.md 时，#10（字段名映射）须显式标注其触发条件（"写入 attempt log entry 时执行"），
使其归入"条件触发"的理由脱离本计划上下文后仍可独立成立。同理，#7（预算追踪）保留在"条件触发"组，
落地时须显式标注触发语义："逐轮递增计数由主循环结构保证；本条规范触上限时必须问用户的行为
（呼应宪法第二部 #3）"——与 #10 的标注处理平行。

**编号保持约束**：分组后每条保留原全局编号 #1-#18 不变，组内不重排、不重新编号。
理由：refs/orchestrator-guide.md §六 按全局编号引用职责条目（5 处"对应职责 #N"），
重新编号会静默破坏该宪法保护文件中的跨文档引用，而 refs/ 不在本计划改动范围内。

**分组标签形式**：用行内加粗（如 "**每轮必做** ——"）而非独立 `###` 子标题与空行，
兼顾行数与可扫描性；若行内加粗损害分组可扫描性，允许改用独立子标题并占用 §五.1 容差。

"收敛完成前必检" 12 条 checkbox 清单（SKILL.md:258-269）保持原位、**不并入上述任何分组**——
"收口必做"组仅以指针引用该清单（如"收口时另行逐项执行'收敛完成前必检'"），
不复制、不迁移其中任何条目，避免两处维护。

本计划是"**重排优先、删减待实证**"两步走的第一步——零删除承诺是本步的边界，
不是对 18 条清单最优性的背书；删减决策由 §四 的效果观察数据驱动（见 §四.4/§四.5）。

### C2（对应 F2）：ultraverge 单一权威定义

- 保留执行流程专节（SKILL.md:124-150）为唯一完整定义。
- frontmatter description **逐字保留现状，不做任何修改**：触发词说明（含 `ultraverge` 触发词）
  与括注微型定义「(评议+收敛+收敛后设计审查)」全部保留。
  理由：frontmatter 是技能触发元数据（harness 据此激活技能），必须保持自足，
  故豁免于"单一权威定义"原则（对应 §五.3 的 frontmatter 例外）。
- **压缩范围严格限定为 ultraverge 相关描述，不触碰任何非 ultraverge 内容**：
  - Positioning 图（SKILL.md:19-23）：该图主体描述产物整个生命周期（草稿 → Round 0 → 评议 →
    Converge → 落地），ultraverge 仅为其中一列注记。**仅**改写该列的 ultraverge 描述文字：
    允许做最小化改写（如 "ultraverge: 全量（见执行流程）"），不强制"一句话 + 指针"的完整重写；
    图中主流程行及评议 / Converge / Round 0 等非 ultraverge 注记**逐字保留，不删不改**
    （图内 ASCII 对齐空白的重排不计入语义变更）。
  - 模式边界注记（SKILL.md:116）：该引文块整体即 ultraverge 描述，整块压缩为一句话 + 同上指针。
- 净减行数为非约束性预期（硬标准见 §五.1：总行数增加不超过 +3 行容差）。

### C3（对应 F3）：合并收敛方式与 D11 两表

合并 SKILL.md:62-68（收敛方式表）与 SKILL.md:72-80（D11 三选一表）为单表，
列：终止类型 | 判据 | 用户确认要求 | 产物要求。
合并后小节标题定为 "## 终止状态与收敛判定（D11）"，同时替换原"## 收敛方式"与
"## 收敛判定（D11 三选一）"两个小节标题。

- **行集承诺（恰为 5 行）**：D11-a（即原"收敛"行，严格首轮通过）、D11-b（渐近）、
  D11-c（主观接受）、预算软停、振荡硬停。
- **"产物要求"列逐行保留原文语义**：原收敛方式表 3 行的产物要求逐行迁入对应行
  （收敛 → 写 retrospective.md、移 done/；预算软停 → retrospective.md 注明"未收敛但用户接受"；
  振荡硬停 → retrospective.md 填病因 + 建议）；D11-b/c 行的产物要求沿用"收敛"行
  （写 retrospective.md、移 done/）。
- **语义区分不得抹平**：D11-b/c 是经用户显式确认的**收敛**，预算软停是"未收敛但用户接受"，
  二者不得合并为一行；预算软停与振荡硬停在 D11 中均无对应判定类型，两行均在映射标注中
  显式标注"无 D11 对应"（预算软停不是收敛）。
- **标签措辞约束**：合并表中 D11-b/c 行的"终止类型"标签不得使用"收敛"一词
  （分别用"渐近通过"/"主观接受"），与 refs/orchestrator-guide.md 的 retrospective 标注规则
  （D11=c 不标"收敛"，标"主观接受"）保持一致。
- **歧义消解规则**（写入表注）：达预算上限后用户接受 → 预算软停；未达上限用户主动接受 → D11-c。
- 原"D11=a 是默认目标。b/c 需用户显式确认"的说明保留。

"收敛/预算软停/振荡硬停" 与 "D11 a/b/c" 的映射关系在表内显式标注，消除读者自行对齐成本。

### C4（对应 F4）：主循环编号修正及同批格式修正

- SKILL.md:185-186 重复的 `f.` 重排为连续编号（f/g/h/i）。
- 同批纯格式修正（零语义变更，与上项一并提交）：
  - SKILL.md:223 "建议在产品涉及系统级设计时启用" 的 "产品" 改为 "产物"（错字）。
  - SKILL.md:152 `###` 标题中混入正文（"### 默认入口：评议。 首次审查一律……"）：
    拆分为标题（"### 默认入口：评议"）+ 独立正文段，文字内容逐字保留。

### C5（对应 F5）：优先级措辞澄清（CONSTITUTION 与 SKILL.md 同步改写）

- CONSTITUTION.md:3-6 改写为单段：明确"设计治理上本文件最高；运行时机制冲突时 SKILL.md 优先，
  冲突本身即触发修宪对齐"。语义不变。
- **改写必须逐项保留以下内容**（允许措辞整合，不允许语义删除）：
  - CONSTITUTION.md:4 的权威来源声明：本文件的权威源于用户对 ultraverge 收敛结果的批准，
    而非内容的生成来源。
  - CONSTITUTION.md:5 的分工声明：机制描述以 SKILL.md 为准，宪法约束以本文件为准。
  - CONSTITUTION.md:3 的定义性从句：定义"SKILL 该长什么样、该包含什么"的宪法级判据。
  - CONSTITUTION.md:6 的对齐方向：冲突通过修宪程序解决——修正本文件（CONSTITUTION.md）
    与 SKILL.md 对齐；新表述须明示该对齐方向（修宪法以对齐 SKILL.md，而非反向）。
- **同步改写 SKILL.md:33-34 镜像句**（"CONSTITUTION.md 是本 SKILL 的最高治理文档。
  冲突时以本文件（SKILL.md）为准——运行时依赖优先于设计参考"）：与 CONSTITUTION.md
  新表述保持措辞一致，在两处同时消除"最高 vs 让位"的表面矛盾，避免产生新的双文档措辞漂移。
  SKILL.md:33 的指针句（"设计原则、Orchestrator 行为边界、治理文档清单、修改程序详见
  CONSTITUTION.md。"）不在改写范围内，逐字保留；改写对象仅为 line 34 的优先级镜像句。

### 改动范围声明

- 涉及治理文档：SKILL.md（C1-C4，及 C5 的 SKILL.md:33-34 同步镜像句）、CONSTITUTION.md（C5）→ 须 ultraverge
- 不涉及：refs/ 全部文件、scripts/、README
- 明确不做：修改修宪程序（F7）、增删任何机制、调整任何默认参数
- **原子性与回滚**：C1-C5（含宪法改动）作为单次原子提交；任一 §五 验收项失败则整体不提交、
  计划退回修订。

---

## 四、运维行动（不属于文档修改，无需 ultraverge）

1. **积累实证**：在接下来 3-5 个真实收敛任务上使用 converge（评议入口即可），产出 retrospective。
2. **启动 distill**：有 ≥2 份新 retrospective 后运行 `python scripts/distill_antipatterns.py`，
   回填注册表统计值，验证 status 衰减机制可运行。
3. **记录 `/goal` 实战数据**：在某次收敛的 inner loop 中实际使用 `/goal` 加速 Executor，
   将体验记入 retrospective（A.1 的描述目前只有文档依据，无本仓库实证）。
4. **C1 效果观察**：后续 3-5 次真实收敛的 retrospective 中记录：(a) "条件触发"组条目是否被
   正确跳过/执行；(b) "每轮必做"组是否每轮全部执行；(c) Orchestrator 是否出现按组定位职责的行为。
   观察结果作为"重排是否改善遵循率"的实证。
5. **第二步触发条件**：若 C1 效果观察显示分组对遵循率无改善，或实证发现 18 条中存在可合并冗余
   （候选：#15/#16/#17 三条门控项、#2/#3 相邻判定与计数），启动第二次修宪评估"真删减"
   （语义变更类，走完整 ultraverge）。

---

## 五、验收标准（供 ultraverge Round 0 合同参考）

1. **硬标准**：C1-C5 全部落地后，SKILL.md 总行数增加不超过 +3 行（容差上限）。
   （非约束性预期：净减 ≥10 行；该预期仅供参考，不作为验收阻断依据。）
   （容差理由：容差的目的是消除"为省 1-2 行而操纵排版"的边缘激励——行数是重量的代理指标，
   不应在边缘处与可读性目标反向。）
   （预算依据：逐项推演净值约在 -2~+3 行间，全部落在容差内，分组标签形式仍是主要调节变量。）
2. 18 条责任清单条目在重组后逐条可追溯（零删除、零语义变更、零外部条目混入——
   Reviewer 应按 C1 映射表逐条 diff 验证，四组合计恰为 18 条，每条恰出现一次）
   （C1 明示的触发语义标注属标注性新增，不计入语义变更）。
3. ultraverge 定义在 SKILL.md 中只有一处完整版本，其余位置为一句话级指针，
   **frontmatter 例外**——按 C2 保留自足的触发词说明；
   Positioning 图中非 ultraverge 内容逐字保留（C2 压缩范围限定；
   图内 ASCII 对齐空白的重排不计入语义变更，见 C2）。
4. 终止状态只有一张表，行集恰为 C3 承诺的 5 行，"产物要求"列逐行可追溯。
5. 主循环步骤编号连续无重复；C4 同批格式修正（SKILL.md:223 错字、SKILL.md:152 标题拆分）
   全部落地且零语义变更。
6. CONSTITUTION.md:3-6 与 SKILL.md:33-34 两处优先级表述均消除"最高 vs 让位"的表面矛盾，
   两处措辞同步一致、语义与现行一致，且 CONSTITUTION.md:3（定义性从句）、
   CONSTITUTION.md:4（权威来源于用户批准）、CONSTITUTION.md:5（机制/宪法分工）与
   CONSTITUTION.md:6（对齐方向）的内容逐项保留（与 C5 保留清单一致）。
7. 全部改动通过 CONSTITUTION 第一部双原则自检（Bitter Lesson：纯机制层整理，无模型补丁；
   Occam：每处改动对应一个具体发现 F1-F5）。
8. refs/orchestrator-guide.md §六 的 5 处"对应职责 #N"编号引用（#5/#6/#7/#12/#14，
   位于该文件 151/158/165/172/180 行）在改动后全部仍可正确解析（逐处验证）。
9. **方向性盲评**：验收 Reviewer 在不被告知哪版是新版的前提下，对"分组重排版"与"原平铺版"的
   责任清单做方向性判断（哪版更易定位"当前阶段应执行的条目"）；判断结果记入验收报告。
   若判定分组版**明显更差**，退回修订；判定相当或更好则通过。
   盲评由独立最小上下文 Reviewer 实例执行——仅提供两版清单文本，不提供本计划及其余上下文，
   以兑现"不被告知哪版是新版"。

---

## 状态

- [x] 计划写入（本文件）— 2026-06-10
- [x] ultraverge：≥3 并行 Reviewer 评议
- [x] （若评议有阻断）完整收敛
- [x] 强制设计审查
- [x] 收敛后修订：设计审查 3 highlights 采纳并落实（H1-H3）— 2026-06-10
- [x] 人工确认提交 — 2026-06-10（commit 2496555 治理原子提交 + 972dfe9 README 独立提交）
- [x] CONSTITUTION 变更记录留痕（仅 C5 涉及宪法本体）
