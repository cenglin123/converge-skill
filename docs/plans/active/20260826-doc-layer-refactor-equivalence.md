# 等价映射对照表 — 文档层重构

持久化《docs/plans/active/20260826-doc-layer-refactor.md》验收 #1 的等价映射对照表。
本步（landing cut 1 + cut 2c）先登记下列行；后续步骤继续追加。

| # | 主题 | 源位置（改动前） | 单源/目标位置（改动后） | 等价性判定 |
|---|------|------------------|------------------------|------------|
| 1 | 目录树 + slug 命名 + 收敛后修订注 | SKILL.md §目录结构（~486–508；reopen 修订注见 ~518） | refs/state-schema.md §目录结构（新增规范节） | 等价：目录树、tmp/active/done/gate 布局、slug 命名 `<YYYYMMDD>-<对象简述>`、done→active（reopen）→重归档 done 与 retrospective 追加不覆盖均保留；仅自引用改为“本文件以下各节” |
| 2 | scripts/README 过渡性头注 | scripts/README.md 第 5–7 行：“SKILL.md 接线…留后续 plan” | scripts/README.md 头部改为“本文件为脚本命令/用法清单的单源；SKILL.md 已接线 orchest.py” | 等价：消除过期“未来计划”声明，改为单源状态声明；设计契约指针保留；补充“不在 CONSTITUTION 保护清单内，须同步维护并走常规评审”维护说明 |
| 3 | 主循环 reserve-round/register-round/cancel-round 命令块 | SKILL.md 主循环 a0+a（原多行命令） | scripts/README.md Loop A 接线示例 | 等价：保留 a0+a 语义——reserve-round 单命令承担 gate reserve + begin-invocation + round 骨架；settle 由 register-round 承担；cancel-round 失败/取消；不得手跑裸 budget_gate；gate 裁决语义（PROCEED/BLOCK/DENY/FAIL_CLOSED）与两 tier per-scope reserve 均由 reserve-round 驱动不变；仅命令参数指针化 |
| 4 | 主循环 record-verdict 命令块 | SKILL.md 主循环 b | scripts/README.md | 等价：骨架/frontmatter 由 reserve-round/register-round 落盘回填、★LLM 判断、record-verdict 替代手写 frontmatter + 裸 ingest-verdict 保留；仅命令参数指针化 |
| 5 | 主循环 finish 命令块 | SKILL.md 主循环 d2 | scripts/README.md | 等价：finish 只执行步骤 0→8 机械序列（verdict 交叉核对/round 连续/全 settle/孤儿恢复/终局 decision/prompt 归位/原子归档/归档后 check），不覆盖必检清单语义项、不代写 retrospective——保留；仅命令参数指针化 |
| 6 | executor 修复轮 reserve/register/cancel 命令块 | SKILL.md 主循环 f | scripts/README.md | 等价：executor spawn 经 orchest.py、consumes=none 不建骨架、register-round 须 --output attempts.md、失败/取消 cancel-round 保留；**attempts.md metadata-only tradeoff 与崩溃窗口官方恢复路径（重跑 register-round --output attempts.md，禁手工补 terminal）保留在 SKILL.md**；仅命令参数指针化 |
| 7 | 盲审 reserve/register/record-verdict 命令块 | SKILL.md 盲审复核 | scripts/README.md | 等价：consuming 角色缺 --round 直接拒绝、盲审独立序列号、Spawn 盲审 Reviewer 不读 attempts.md、register、verdict 经 record-verdict 且 pass/fail/waived 只进 retrospective——保留；仅命令参数指针化 |
| 8 | inner-loop continue reserve/register/cancel 命令块 | SKILL.md Inner Loop | scripts/README.md | 等价：continue 不携带 reservation、parent 链、max_inner_loops=3、不推进 max_outer_loops、实例冲突拒绝、verdict 经 record-verdict——保留；仅命令参数指针化 |
| 9 | 配置参数表预算参数数值单源化 | SKILL.md §配置参数（max_outer_loops/max_blind_rechecks/ultraverge_min_reviewers/max_inner_loops/impl_severity_streak_threshold/preflight_code_block_threshold/total_safety 的值列） | scripts/budget_gate.py DEFAULTS（单源） | 等价：值列改“见 budget_gate.py DEFAULTS（单源）”；参数名+语义+调优脚注引用保留；preflight_code_loc_threshold 无行不新增；max_total_reserved_spawns 规范公式仍留在 SKILL.md（脚本派生强制上限），不指向 DEFAULTS |
| 10 | 判断侧阈值保留数值 | SKILL.md §配置参数（type_o_threshold/type_r_threshold/plan_drift_check_interval/converge_dir/gate_l1_interval/gate_l2_mode/gate_l2_signal_threshold/gate_max_token_share/executor_model_tier/max_ultraverge_initial/relay_oscillation_interval/task_tier/task_envelope_*） | 无（判断侧阈值，未经脚本强制，按宪法第一部由 Orchestrator 判断侧设定） | 等价：数值/文本原样保留于 SKILL.md 表内；不指向脚本 DEFAULTS |
| 11 | 调优史脚注重定向 | SKILL.md [^mbr]、[^totalcap] | scripts/budget_gate.py DEFAULTS 注释 + git 历史（提交 0137fce） | 等价：脚注不再指向 docs/CHANGELOG.md（无对应 2026-08-16 调优条目）；调优历史单源指向 DEFAULTS 注释 + git 0137fce |
| 12 | ultraverge 模式行为事实（从脚注析出） | SKILL.md [^mbr] 中的 ultraverge 覆盖回写事实 | 留于 SKILL.md 配置表区域（NOTE） | 等价：ultraverge 初始化 config 覆盖 max_blind_rechecks=2、total 上限按规范式重算为 62 作为行为注保留在 SKILL.md 正文；不并入调优史脚注 |
| 13 | 陈旧数字对齐 | SKILL.md Ultraverge-path（原 168 行：真默认 1、42→44） | scripts/budget_gate.py DEFAULTS（max_blind_rechecks=3）+ 规范公式（覆盖 mbr=2 → 62） | 等价：对齐为“普通真实默认 3；ultraverge 覆盖 2；普通 63→ultraverge 62”，消除 42/44/1 陈旧冲突 |
| 14 | 目录结构迁移+摘要 | SKILL.md §目录结构 | refs/state-schema.md §目录结构（新增规范节） | 等价：SKILL.md 留 3 行摘要（tmp/active/done/gate 布局、slug 命名、done→active→重归档+retrospective 不覆盖）+指针；完整树与 slug/修订注在 state-schema 单源 |
| 15 | Archive Contract v1 收缩 | SKILL.md §Archive Contract v1 | refs/state-schema.md §Archive Contract v1（规范单源） | 等价：保留不变量要点（append-only events、provenance 诚实、archive-only 完成门禁、SHA-256 非来源认证）；细节单源在 state-schema |
| 16 | M-11 去重 | SKILL.md M-11（best-effort-guarded hook 接线 bind/unbind/refresh-cap 命令级细节） | refs/framework-adapters/claude-code.md §A.1（wiring 单源） | 等价：M-11 保留责任语义（gate 生命周期经 orchest.py、非 PROCEED 停止、per-scope 预算/mode-switch/extension、混合后端检查点、宿主能力矩阵、auditable-only 无机械兜底）；仅绑定/刷新/解绑/PreToolUse 命令级描述收缩为语义+指针 |
| 17 | C-20 去重 | SKILL.md C-20 | refs/orchestrator-guide.md §盲审复核编排 | 等价：C-20 保留职责要点（≥2 轮触发、Spawn 盲审 Reviewer、BR- 注入、pending 硬过期、blind_recheck 标注）；与 guide §盲审复核编排逐字重复的操作细节收缩为职责要点+指针 |
| 18 | C-21 去重 | SKILL.md C-21 | refs/orchestrator-guide.md §落地执行编排 | 等价：C-21 保留职责要点（Spawn executor、不得直接编辑、instance_id 留痕、清单项数核对、audit plan-landing 建议与非豁免）；与 guide §落地执行编排逐字重复的操作细节收缩为职责要点+指针 |
| 19 | 传话编排收缩 | SKILL.md §传话编排 | 操作细节→refs/orchestrator-guide.md §八；记录格式→refs/state-schema.md §relay-ledger（唯一单源） | 等价：保留适用三条件、载荷文件引用强制、独立性降级声明、角色边界、振荡裁判 spawn 语义（输入仅 relay-ledger、输出仅 Type O/R/F/S、落 total_safety 余量）；仅操作/包裹格式与记录字段指针化 |
| 20 | Archive/reopen 归档命令序列收缩 | refs/orchestrator-guide.md §Archive / reopen 操作（`archive .converge/active .converge/done <slug>` 分步命令） | scripts/README.md（Loop A）+ `orchest.py finish`（单命令）+ `archive_convergence.py --help` | 等价：归档命令序列被 `finish` 取代；保留 per-spawn begin/complete/recover 生命周期契约（reserve 先、begin 先于 spawn、宿主返回后 complete/recover、terminal settlement ref 指 ledger、缺 receipt/resolved model 记 degradation 不猜测）、reopen 保存旧 manifest revision 后追加式、journal 幂等恢复（重试同一命令，禁手工删 source/backup/staging/journal）、绑定唯一性（ledger/registry/round-log/显式 mapping；不可唯一绑定即停，不按文件名猜 role/model）、bootstrap staging-only、legacy done 只读 |
| 21 | §六.5 reserve-round/register-round/continue 命令序列收缩 | refs/orchestrator-guide.md §六.5 预算追踪+gate 编排（command args 明细） | scripts/README.md Loop A（命令名+参数单源） | 等价：保留每次 spawn 经 orchest.py（reserve=gate reserve+begin+骨架、register=complete+settle+回填、cancel 失败/取消）；非 PROCEED 处置（BLOCK 菜单、MODE_SWITCH_REQUIRED 选项、DENY、FAIL_CLOSED 永不 fail-open）；budget_extension 令牌约束（关联真实 BLOCK decision 事件+user_quote+monotonic 链）、孤儿 reservation、ingest-verdict 用法、不得手跑裸 budget_gate reserve/settle；仅命令参数指针化 |
| 22 | §八 checkpoint-paths 用法细节收缩 | refs/orchestrator-guide.md §八 脚本化映射表（checkpoint-paths --commit 前置/降级细节） | scripts/README.md ⑥ checkpoint-paths + guide §八 N-vs-M 判断性核对 | 等价：保留 N vs M 判断性核对（LLM 读 plan 改动清单完成比较，向用户报告差异）；checkpoint-paths 的 commit 前置与无 commit 降级为 LLM 人工核对等操作细节指针化；显式不适用（reserve/register/cancel/record-verdict/finish 不适用落地）保留 |
| 23 | relay 记录字段集去重 + 字段级等价映射 | refs/orchestrator-guide.md §八 relay "#### 2. 记录字段"（timestamp/sender_role/round_id/artifact_path/content_hash/verdict_or_response） | refs/state-schema.md §relay-ledger（唯一格式源） | 等价：字段集从 guide 移除，记录格式以 state-schema §relay-ledger 为唯一单源；**timestamp 字段归属裁决**：guide 曾含 timestamp，state-schema 五字段集不含——裁决为 **timestamp 不属于 relay-ledger 规范记录字段**（时间由 append-only 次序承载），不补入 state-schema；**五组中英字段名映射**在本行裁决并记入本表：发送方/sender_role、轮次/round_id、产物路径/artifact_path、内容hash/content_hash、结论摘要/verdict_or_response |
| 24 | §十 converge_loop.py 与 vault 侧 adapter driver 区分 | refs/orchestrator-guide.md §十 自主循环驱动器 | 保留于 guide §十（adapter-layer 边界）+ scripts/README.md §converge_loop.py | 等价：保留 adapter-layer 边界语句（驱动器实现归属调用方适配层，当前参考=vault 侧 ocsr_driver_core.py + ocsr_dispatch drive）；新增区别句——本仓库 scripts/converge_loop.py 为 converge 侧机械合并器（subprocess 调 orchest.py + 外部 ocsr_dispatch），与 vault 侧 adapter driver 不同域，可选调度器，语义判定按 SKILL.md 主循环，且单活约束（spec 驱动与人工主循环不得并发驱动同一 active 目录）|

---

> 后续步骤（SKILL.md 去机械化、orchestrator-guide.md 去重等）在此表追加行，作为验收 #1 的持久化记录。
